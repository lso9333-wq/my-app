# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 언어 지침

- 결과값(답변, 요약, 커밋 메시지 등 사용자에게 보여지는 텍스트)과 설명은 항상 한국어로 작성한다.
- 코드, 변수명, 파일명 등 코드 자체의 관례는 기존 방식(영어)을 따른다.

## Commands

- `npm run dev` — run Vite dev server + API server together (via `concurrently`)
- `npm run dev:client` — Vite dev server only (HMR)
- `npm run dev:server` — API server only, watch mode (`tsx watch server/index.ts`)
- `npm run build` — type-check the frontend (`tsc -b`, scoped to `src/`) then production build via `vite build`
- `npm run start` — run the API server against the built `dist/` (serves frontend + API from one process)
- `npm run typecheck:server` — type-check `server/` (separate `tsconfig.json`, not part of `npm run build`)
- `npm run lint` — run Oxlint
- `npm run preview` — preview the production build locally
- `npm test` — run the vitest suite once (`vitest run`); `npm run test:watch` — watch mode. See "테스트 러너 도입" below — coverage is intentionally limited to pure computation functions.

## Architecture

Vite + React 19 + TypeScript SPA with a minimal Express + SQLite backend. The frontend is organized **feature-first** (Impakt류 앱의 구조를 참고: 홈 대시보드 + 하단 탭 내비게이션 + 기능별 폴더), with a `shared/` layer for the pose pipeline and cross-feature UI:

```
src/
├── app/            App.tsx(셸: 헤더 + 탭 라우팅) + App.css
├── navigation/      BottomNav.tsx — 홈/보행/ROM/손·발/XMSK/뇌파/XCTS 하단 탭바
├── features/
│   ├── home/       HomeScreen.tsx — 대시보드(기능 카드 + 최근 ROM 기록 미리보기)
│   ├── gait/       보행 분석 (GaitApp.tsx, components/, lib/, types.ts)
│   ├── rom/        스트레칭 ROM 분석 (RomApp.tsx, components/, lib/, types.ts)
│   ├── handfoot/   손·발 분석 (HandFootApp.tsx, HandAnalysisPanel.tsx, FootAnalysisPanel.tsx, components/, lib/, types.ts)
│   ├── xmsk/       XMSK 트레이너 도구 (XmskApp.tsx, components/, lib/, types.ts)
│   ├── eeg/        뇌파(EEG) 실시간 표시 (EegApp.tsx, components/, lib/, types.ts) — 서버 저장 없는 클라이언트 전용 기능, 아래 별도 절 참고
│   └── xcts/       XCTS 심혈관/전신 컨디션 측정 도구 모음 (XctsApp.tsx, components/, lib/, types.ts) — 아래 별도 절 참고
├── shared/
│   ├── lib/        poseDetector.ts, handDetector.ts, mathUtils.ts, chartUtils.ts, heartRate/(bleHeartRate.ts 등, XCTS·향후 외부기기 연동 공용)
│   ├── components/ VideoUploader.tsx, ComparisonBarChart.tsx
│   └── types/      pose.ts (Side/Point/PoseFrame), hand.ts (HandFrame)
└── shims/          mediapipe-pose-shim.ts, mediapipe-hands-shim.ts
```

These features share the pose pipeline(s) / shell:

1. **보행 분석 (gait analysis)** — analyzes a walking video entirely client-side.
2. **스트레칭 가동범위 분석 (stretch ROM analysis)** — analyzes one video where the user marks a "before" and "after" stretching segment, and computes joint range-of-motion change. Only the *computed numeric results* are sent to the backend for storage — raw video and per-frame pose data never leave the browser, in both features.
3. **손·발 분석 (hand/foot analysis, 2026-09)** — like ROM, marks a "before"/"after" segment in one video, but uses a *different* pose model for hands (MediaPipeHands, 21 landmarks) and reuses the body-pose model for a whole-foot experimental estimate. See the dedicated section below.
4. **XMSK** — a password-gated, trainer-only reference tool (pain-relief "recipes", a muscle dictionary, and a trainer skills evaluation form). Unrelated to the pose pipeline; entirely server-backed.
5. **뇌파(EEG) 실시간 표시 (2026-09)** — 카메라/포즈 파이프라인과 무관. Web Bluetooth로 소비자용 Muse EEG 헤드밴드에 연결해 실시간 파형과 대역 파워를 보여준다. 다른 네 기능과 달리 서버에 전혀 저장하지 않는 100% 클라이언트 전용 기능이다. See the dedicated section below.
6. **XCTS — 심혈관/전신 컨디션 측정 도구 모음 (2026-09)** — 카메라/포즈 파이프라인과 무관. 표준 BLE 심박 센서를 연결해 활동 전/후 심박수·HRV 변화를 측정·저장한다. 뇌파(EEG)에 이은 두 번째 외부 생체측정기기 연동이지만, EEG와 달리 서버에 세션을 저장한다(회원별 기록이 필요하다는 명시적 요구) — XMSK 안에 두지 않고 별도 최상위 탭·별도 세션 저장소로 분리했다. See the dedicated section below.

### Frontend

- `src/main.tsx` — entry point, mounts `App` (`src/app/App.tsx`) into `#root`
- `src/app/App.tsx` — thin shell: header (current tab title) + `<main>` tab switch between `HomeScreen`, `GaitApp`, `RomApp`, `XmskApp` + fixed `BottomNav`
- `src/navigation/BottomNav.tsx` — 홈/보행/ROM/XMSK 하단 탭바 (`AppTab` 타입을 export, `App.tsx`와 `HomeScreen`이 공유)
- `src/features/home/HomeScreen.tsx` — 랜딩 대시보드: 기능 카드 3개(탭으로 이동) + `romApi.listRomSessions(3)`으로 가져온 최근 ROM 기록 미리보기. 대시보드 성격상 `rom` 기능의 `lib`/`types`를 직접 참조하는 유일한 교차 참조.
- `src/app/App.css` / `src/index.css` — styling (shared design tokens in `index.css` `:root`); `src/navigation/BottomNav.css`, `src/features/home/HomeScreen.css`는 각 컴포넌트가 자체 로드.

**Shared pose pipeline** (`src/shared/lib/poseDetector.ts`): a singleton BlazePose(`lite`, `runtime: 'tfjs'`, webgl backend) detector, 33 랜드마크(발뒤꿈치·발끝 포함) — MoveNet(COCO-17)에서 업그레이드한 것으로, 발이 실제로 바닥에 닿는 지점을 더 정확히 추정하기 위해 heel/foot_index 키포인트를 추가로 쓴다. `lite`를 쓰는 이유는 저사양 모바일 GPU에서도 프레임마다 안정적으로 사람을 재검출하기 위함 — 더 무거운 `full`/`heavy`는 다리가 서로 겹치는 프레임(보행 영상에 흔함)에서 검출 자체를 자주 놓쳤다. 키포인트는 고정 인덱스가 아니라 각 키포인트의 `name` 필드로 조회한다(모델별로 개수·순서가 다르므로). 프레임을 채택하는 기준은 골반(hip) 신뢰도만 필수로 요구하고 발목/발은 필수로 두지 않는다 — 옆에서 찍은 보행 영상은 걸음마다 한쪽 발이 반대쪽 다리에 가려 순간적으로 신뢰도가 떨어지는 게 정상이라, 여기까지 필수로 두면 유효 프레임이 급감했다(가려진 프레임의 발 좌표는 `gaitAnalysis.ts`가 직전 값으로 유지해 튀는 값을 걸러낸다). `extractPoseFrames(video, opts, debugStats?)` samples a `<video>` element at a target `opts.samplingFps` starting at `opts.startTimeSec` (default 0) for up to `opts.maxDurationSec` (used by both features, with different time windows). `extractViaPlayback` uses a **producer/consumer** architecture: it seeks once to the start point, then calls `video.play()` exactly **once** for the whole extraction and never pauses it mid-stream. Every `requestVideoFrameCallback` firing is the "producer" — if enough of the sampling interval (`1/samplingFps`) has elapsed since the last capture, it immediately (synchronously) draws the current frame into a fresh `<canvas>` and pushes `{ time, canvas }` onto a queue (capped at `MAX_CAPTURE_QUEUE = 6` to bound memory if inference can't keep up — excess captures are skipped, not substituted). A separate "consumer" loop drains that queue one item at a time, running pose estimation on each captured canvas. Falls back to the older repeated-`currentTime`-seek loop, `extractViaSeek`, only when `requestVideoFrameCallback` is unavailable. This design is **not** seek-per-sample, **not** free-running playback with inference happening on the live (still-advancing) video, and — importantly — **not** pause-during-inference. It went through three problems, in order, each fixing the last: (1) *frame accuracy* — some Android/HEVC video + browser combinations don't seek frame-accurately (`currentTime = t` snaps to the nearest keyframe), so requesting many different timestamps in a short clip can silently re-decode the *same* handful of frames over and over — pose detection still finds a person every time (frame "adoption rate" looks fine), but the joint-position time series is nearly flat, so gait step-detection finds almost nothing. Playing forward and reading `requestVideoFrameCallback`'s `mediaTime` (instead of seeking) sidesteps this. (2) *run-to-run non-determinism* — letting the video free-run continuously while inference happened in the background meant that, since inference speed varies run-to-run with device load/thermal state but the video keeps advancing at 1x regardless, a slower inference run let more real playback time pass per sample, so different runs of the *same* video sampled different actual frames (different step counts/symmetry each time). (3) *play()/pause() race* — the first fix for (2) tried "play to target → pause on that exact frame → run inference while paused → resume", but calling `video.pause()` while a `video.play()` request is still pending causes the browser to treat that play() request as interrupted, destabilizing the playback pipeline; fast hardware (PC) hit this far more often (target frames arrived before `play()`'s own promise had even resolved), which is what caused PC's near-total extraction failures (ROM "데이터 부족", gait's skeleton playback effectively disabled) — phones were slower and hit the race less often, but still enough to keep producing different frame samples run to run. The current producer/consumer design fixes (3) by never pausing mid-playback at all, while still fixing (2): frame *selection* is driven purely by real playback progress (deterministic, decoupled from inference speed) and frame *pixels* are captured synchronously into a canvas at the moment of selection (so slow inference downstream can never change which pixels ended up being analyzed). (4) *"supported" but silent rVFC, and unbounded waits* — after shipping (3), some PC environments showed the extraction never completing / the analyzed video never visibly playing: `typeof video.requestVideoFrameCallback === 'function'` being true does not guarantee the browser actually invokes it every frame on every GPU/driver combination, and since both frame capture *and* the `mediaTime >= end` stop condition live inside that callback, a browser that never calls it means the code has no way to know the target segment ended — it would keep waiting for the *entire underlying video* to reach its real `ended` event (which, for a short marked sub-range of a long video, can look like a permanent hang). `extractViaPlayback` now guards against this two ways: if zero rVFC callbacks arrive within `RVFC_STARTUP_GRACE_MS` (1.5s) of calling `play()`, it aborts and returns `null`, and `extractPoseFrames` transparently retries the same segment with `extractViaSeek`; independently, a `watchdogMs` time budget (scaled to the segment length) force-finishes the extraction with whatever frames were gathered so far if it somehow runs long regardless. `seekTo` also no longer waits forever on a `'seeked'` event that some browsers skip when the seek target is very close to the current time (resolves immediately for near-zero seeks, and has a 1.5s fallback timer besides). The optional third argument, `debugStats: ExtractDebugStats`, is mutated in place with per-run diagnostics (attempted/no-pose/dropped/kept frame counts, per-keypoint average confidence, video resolution, and `sampledTimesChecksum` — the sum in ms of every attempted frame's `mediaTime`, added specifically so two runs of the *same* video on the *same* device can be compared: if the checksum matches but step counts/symmetry still differ, frame selection is stable and the remaining variance is downstream of extraction — e.g. inference itself, or the compositor not consistently presenting frames for the near-invisible analysis `<video>` — rather than in `extractViaPlayback`) — surfaced in `GaitMetricsPanel`'s empty state and persisted via `gaitDiagnosticsApi`/`GaitDiagnosticsHistory` (see below) so detection failures (and now run-to-run drift) can be diagnosed without a remote debugger. A video element being analyzed must stay out of `display:none` (some browsers stop firing `requestVideoFrameCallback` for non-rendered video) — `GaitApp`'s hidden analysis `<video>` is kept technically visible (1–2px, near-zero opacity, fixed position) rather than using the `hidden` attribute, for exactly this reason. (5) *PC-vs-phone result divergence even after (1)–(4)* — once playback extraction was reliable on both PC and phone, the same video could still produce visibly different final numbers (gait cadence/symmetry, ROM angles) between a PC run and a phone run. Two further, independent changes target this, though neither eliminates it completely: first, `extractViaPlayback`'s producer used to capture whenever "time since last capture ≥ interval," a greedy scheme where each device's own decode frame rate causes the sampling grid to drift by a slightly different amount, so PC and phone end up sampling different actual timestamps by the end of a clip; it now targets a **fixed absolute time grid** (`nextTarget` starting at `startTimeSec`, advanced by exactly `interval` each capture — `while (nextTarget <= metadata.mediaTime) nextTarget += interval`), so both devices aim at the same nominal timestamps regardless of decode speed. Second, `extractPoseFrames()` now runs every frame's coordinates through a **One Euro Filter** (`src/shared/lib/oneEuroFilter.ts`, `smoothPoseFrames()` — Casiez, Roussel & Vogel, "1€ Filter: A Simple Speed-based Low-pass Filter for Noisy Input in Interactive Systems," CHI 2012, doi:10.1145/2207676.2208639) before returning frames to gait/ROM/XMSK-estimate consumers, independently per keypoint per axis, using each frame's real timestamp as the adaptive clock — this damps the frame-to-frame coordinate jitter that differs between devices due to GPU floating-point non-determinism (Morin & Willetts, "Non-Determinism in TensorFlow ResNets," arXiv:2001.11396, 2020) and hardware video-decoder differences (an acknowledged cross-device landmark-divergence issue on MediaPipe's own repo: google-ai-edge/mediapipe#2497), without adding perceptible lag to real movement. **Neither change makes PC and phone produce identical numbers** — the root cause (different GPUs/decoders literally computing slightly different pixel/landmark values for "the same" frame) is a hardware-level non-determinism problem that pose-estimation research has not published a full fix for; these two changes narrow the resulting drift in the final metrics without claiming to close it. `debugStats.sampledTimesChecksum` remains computed from the pre-smoothing, pre-any-of-this raw frame times, so it still isolates "did we pick the same timestamps" from "did those timestamps decode to the same pixels/landmarks" when comparing diagnostic records across devices. Also exports `POSE_CONNECTIONS` (skeleton edge list, 발 부분 포함) used by `SkeletonViewer`.

- `src/shared/types/pose.ts` — `Side`, `Point`, `PoseFrame` (hip/knee/ankle/shoulder/elbow/wrist + heel/footIndex keypoints + raw `keypoints[]`) — used by both gait and ROM.
- `src/shared/lib/mathUtils.ts` — shared `mean`/`stddev`/`movingAverage`, used by both `gaitAnalysis.ts` and `romAnalysis.ts`.
- `src/shared/lib/chartUtils.ts` — shared `niceMax` (축 최대값 라운딩), used by both chart components.
- `src/shared/components/VideoUploader.tsx` — drag/drop + tap-to-pick file input (shared by `GaitApp`/`RomApp`).
- `src/shared/components/ComparisonBarChart.tsx` — generic left/right bar comparison (`leftLabel`/`rightLabel` optional) — reused by gait (좌우 비교), ROM (이전/이후), XMSK (이전/이후 측정값).
- `src/shared/components/PdfExportButton.tsx` / `PrintReportHeader.tsx` — "PDF로 다운로드" 버튼을 보행/ROM/XMSK 결과 화면에 공통으로 붙인다. 새 PDF 라이브러리를 추가하는 대신(이 프로젝트는 테스트 러너도 없이 의존성을 최소로 유지하는 방침이고, 이 개발 환경은 네트워크가 막혀 있어 새 npm 패키지를 추가해도 `npm ci`가 요구하는 package-lock.json을 실제로 설치해보며 갱신할 방법이 없다) 모든 브라우저가 이미 지원하는 인쇄 기능("인쇄 → PDF로 저장")을 재사용한다. `PdfExportButton`은 클릭 시 `targetRef`가 가리키는 요소에만 `printable-report-active` 클래스를 붙이고(그 사이 `document.title`도 파일명 제안용으로 바꿨다가 되돌린다) `window.print()`를 호출한다 — `App.css`의 `@media print` 규칙이 그 클래스가 붙은 요소만 보이도록(`visibility` 트릭으로 나머지를 전부 숨기고 그 요소만 페이지 좌상단에 배치) 만들고, 다크 테마 색상 변수를 인쇄 시에만 밝은 배색으로 바꿔치기해 흰 종이에 인쇄해도 잘 보이게 한다(개별 컴포넌트가 이미 전부 `var(--text)` 등을 쓰고 있어 `:root` 재정의만으로 전체가 자연스럽게 인쇄용 배색이 된다). `.no-print` 클래스를 붙인 요소(버튼 자신, 다시 분석하기/삭제 버튼, 저장 상태 문구 등)는 인쇄에서 항상 빠진다. `PrintReportHeader`는 제목 + 회원/트레이너 이름 + (영상명 등 부가정보) + 저장 시각을 보여주는 공통 헤더로, 화면에도 그대로 보이고 PDF에도 그대로 포함된다. 여러 결과 패널이 동시에 화면에 떠 있어도(예: 방금 분석한 결과 + 과거 기록 상세) 클릭한 패널의 ref에만 클래스를 붙이므로 그 패널만 인쇄된다. 적용 위치: `GaitApp`(영상 재생/스켈레톤 뷰어는 제외하고 수치 결과+차트만 — 헤더 바로 다음에 버튼을 둬 ROM과 같은 위치로 맞췄다), `GaitDiagnosticsHistory`의 진단 기록 상세, `RomResultsPanel`(내부에 자체 내장 — `RomApp`의 방금 분석 결과와 `RomSessionHistory`의 저장 기록 상세 양쪽에서 자동으로 버튼이 뜬다), `XmskRecipeFlow`의 결과 요약 단계, `XmskSessionHistory`/`XmskEvaluationHistory`의 기록 상세.
  - **`GaitApp`의 "방금 분석한 결과" 화면과 `GaitDiagnosticsHistory`의 "진단 기록" 상세는 서로 다른 화면이다** — 둘 다 각자 PDF 버튼이 있어 혼동하기 쉬웠다(실제로 사용자가 "세 번째 화면(차트)이 빠졌다"고 신고한 게 알고 보니 진단 기록 쪽을 테스트한 것이었다). `GaitDiagnosticRecord`는 애초에 기기별 감지 상태를 비교하기 위한 요약 통계(프레임 수, 신뢰도, 체크섬 등)만 저장하고 걸음 간격 시계열/좌우 대칭성 원본값은 저장하지 않으므로, 그 상세 화면의 PDF는 설계상 항상 텍스트 1페이지뿐이고 차트가 없다 — 버그가 아니다. 걸음 간격 추이·좌우 비교 차트가 포함된 전체 리포트는 `GaitApp`이 `stage === 'done'`일 때(영상을 분석한 직후, 페이지를 벗어나기 전) 나오는 화면에만 있다. 사용자 혼동을 줄이기 위해 `RecordDetail`에 이 차이를 설명하는 안내 문구(`no-print`)를 추가했다.
  - **카카오톡 인앱 브라우저 대응**: 카카오톡 등 앱 내장 웹뷰는 `window.print()`를 완전히 무시하는 경우가 있어(실제로 재현·확인됨) 버튼을 눌러도 아무 반응이 없다 — 웹 코드로 고칠 수 있는 문제가 아니라 사용자를 실제 브라우저로 내보내야 한다. `PdfExportButton`은 `navigator.userAgent`에 `KAKAOTALK`이 포함되면(카카오톡 인앱 브라우저 감지) 인쇄 버튼 대신 `kakaotalk://web/openExternal?url=...` 스킴으로 현재 페이지를 기본 브라우저로 여는 안내 버튼을 보여준다.
  - **모바일(안드로이드) Chrome에서 인쇄 화면에 내용이 안 들어오는 문제 — JS 기반 격리로 보강**: 카카오톡→외부 브라우저로는 넘어가지만 정작 안드로이드 Chrome에서 인쇄를 실행하면 미리보기에 내용이 비는 신고가 있었다. `body * { visibility:hidden }` + `:has()` 기반 `display:none` 규칙(위 항목)은 데스크톱 최신 Chrome/Safari에서는 잘 동작하지만, 일부 안드로이드 기기의 시스템 웹뷰는 `:has()`를 지원하지 않을 수 있어(지원 안 하면 그 규칙 전체가 조용히 무시된다) 보호 장치가 안드로이드에서만 빠질 가능성이 있다. 그래서 CSS와 별개로 `PdfExportButton`에 `isolateForPrint(el)`을 추가했다 — 클릭 시점에 JS로 인쇄 대상에서 `<body>`까지 조상을 하나씩 올라가며 매 단계의 "형제 요소들"에 인라인 `display:none !important`를 직접 걸어(특정 CSS 기능 지원 여부와 무관하게 항상 동작하는, 훨씬 오래되고 널리 호환되는 방식) 문서 흐름에서 완전히 제거하고, 인쇄가 끝나면 원래 인라인 스타일로 복원한다.
  - **실제 원인은 버그가 아니라 "잘못된 인쇄 진입점"이었다**: 위 두 가지(`:has()` CSS, JS `isolateForPrint`)를 모두 배포한 뒤에도 안드로이드 크롬에서 여전히 빈 페이지가 나온다는 신고가 이어졌는데, 사용자가 보내준 스크린샷 단계별 캡처로 실제 원인이 드러났다 — 사용자가 앱 안의 "PDF로 다운로드" 버튼이 아니라 **크롬 자체의 브라우저 메뉴(⋮ → "인쇄/PDF")** 로 인쇄를 시도하고 있었다(그것도 결과 화면이 아니라 홈 화면에서). 이 경로로는 `PdfExportButton`의 클릭 핸들러가 전혀 실행되지 않으므로 `printable-report-active` 클래스가 어디에도 붙지 않고, 그러면 `body * { visibility:hidden }` 규칙만 무조건 적용되어 정말로 완전히 빈 페이지가 인쇄된다 — 코드가 고장난 게 아니라 이 구현 방식의 당연한 결과였다. 이름이 똑같이 "인쇄/PDF"라 헷갈리기 아주 쉬운 상황이었다. 재발 방지를 위해 `App.tsx`에 항상 렌더링되는 `#print-fallback-notice`를 추가했다 — `.printable-report-active`가 문서 어디에도 없는 상태로 인쇄가 실행되면(`body:not(:has(.printable-report-active)) #print-fallback-notice`) 완전히 빈 종이 대신 "웹페이지 메뉴가 아니라 결과 화면 안의 PDF로 다운로드 버튼을 눌러달라"는 안내 문구가 대신 인쇄되도록 했다. 이 override 규칙은 반드시 위의 "형제 요소 제거" 블랭킷 규칙보다 명시도가 높아야 하는데(그렇지 않으면 안내 문구의 조상들까지 display:none으로 같이 사라진다), 클래스 선택자만으로는 블랭킷 규칙의 `:not():not():not()` 조합이 이미 더 높은 명시도를 갖고 있어 안내 문구가 계속 안 보이는 문제가 있었다 — 그래서 `id="print-fallback-notice"`를 쓰고, 블랭킷 규칙 자체에도 `:not(#print-fallback-notice):not(:has(#print-fallback-notice))`를 추가해 안내 문구와 그 조상 체인을 명시적으로 예외 처리했다.
  - **실제 정확한 버튼을 눌렀는데도 안드로이드에서 빈 페이지 — `afterprint`가 실제 캡처보다 먼저 온다**: 사용자가 진짜로 (카카오톡이 아닌) 외부 크롬에서, 홈이 아닌 정확한 결과 화면에서, 진짜 "PDF로 다운로드" 버튼을 눌렀는데도 인쇄 화면에 `#print-fallback-notice`(내용 없음 안내)가 뜨는 것까지 스크린샷으로 확인됐다 — 즉 버튼의 클릭 핸들러 자체는 실행됐지만, 실제 안드로이드 OS 인쇄 화면이 페이지를 캡처하는 시점에는 이미 `printable-report-active` 클래스가 사라진 상태였다는 뜻이다. 원인으로 추정되는 것: 안드로이드 크롬은 `window.print()`를 호출하면 실제 렌더링을 별도의 OS "인쇄" 시스템 액티비티로 넘기는데, 크롬이 `afterprint` 이벤트를 그 OS 화면이 실제로 내용을 캡처하기 훨씬 전에(인쇄 요청을 안드로이드에 넘겨준 시점에 곧바로) 발생시키는 경우가 있는 것으로 보인다 — 그러면 `afterprint`를 곧바로 신뢰해 `cleanup()`으로 클래스를 떼어버리는 기존 코드가, 실제 OS 캡처보다 먼저 원래 상태로 되돌려버려 빈 페이지가 찍힌다. `PdfExportButton`은 이제 `afterprint`가 와도 곧바로 정리하지 않고 4초 더 기다린 뒤에 정리하며(`afterprint`가 아예 안 오는 경우에 대비한 상한선도 5초 → 10초로 늘렸다), `printable-report-active`는 `@media print` 밖에서는 아무 효과가 없어 정리가 몇 초 늦어져도 화면상 티가 나지 않는다는 원래 전제를 그대로 활용한 수정이다. 다만 이 타이밍 가설은 실제 안드로이드 기기 없이는 100% 확정할 수 없어, 배포 후 실제 재현 여부 확인이 필요하다.
  - **진단 기록 PDF에 "2페이지"로 방금 분석한 결과와 같은 통계/그래프 추가**: 사용자가 명시적으로 요청한 기능 — `GaitDiagnosticCreateRequest`/`GaitDiagnosticRecord`(프론트 `src/features/gait/types.ts`, 서버 `server/types.ts`)에 선택 필드 `gaitMetrics`(프론트엔드 `GaitMetrics` 전체)를 추가했고, `GaitApp`이 진단 기록을 저장할 때(`saveGaitDiagnostic` 호출) 방금 계산한 `computed`(GaitMetrics)를 함께 보낸다. 서버는 다른 `*_json` 컬럼과 같은 방식으로 `gait_diagnostics.gait_metrics_json`에 그대로 JSON 문자열로 저장/반환만 하고 내용을 검사하지 않는다(`server/db.ts`의 `ensureColumn`으로 컬럼 추가 — 기존 행은 NULL). `GaitDiagnosticsHistory`의 `RecordDetail`은 `record.gaitMetrics`가 있으면 `GaitMetricsPanel`/`StepIntervalChart`/`ComparisonBarChart`(모두 `GaitApp`의 결과 화면과 동일 컴포넌트 재사용)를 `.gait-diagnostic-page2`로 감싸 렌더링하고, 이 클래스에는 `@media print`에서 `break-before: page`를 걸어 1페이지 내용 분량과 무관하게 항상 새 페이지(2페이지)에서 시작하도록 강제한다. 이 컬럼이 생기기 전에 저장된 기존 기록은 `gaitMetrics`가 null이라 2페이지가 생기지 않고, 대신 "예전 버전이라 그래프 데이터가 없다"는 안내 문구만 보여준다 — 과거 기록을 소급해서 채울 방법은 없다(원본 프레임 데이터를 보관하지 않으므로). 헤드리스 Chromium으로 "1페이지 요약 + break-before:page 2페이지 상세" 구조를 재현해 정확히 2페이지로, 페이지 경계도 의도한 지점에서 나뉘는 것을 확인했다.
  - **`window.print()`는 반드시 클릭 핸들러 안에서 동기 호출**: 한때 "캡처 타이밍" 가설(클래스를 붙인 직후 곧바로 인쇄를 시작하면 새 레이아웃이 페인트되기 전에 캡처된다)을 의심해 `el.offsetHeight` 강제 리플로우 + 중첩 `requestAnimationFrame` 뒤에 `window.print()`를 호출하도록 바꿔본 적이 있다. 이 변경은 실제로 배포해 확인해보니 데스크톱 브라우저의 "세 번째 화면 누락" 증상은 그대로 남아있으면서, 폰에서는 버튼을 눌러도 아무 반응이 없는 새로운 회귀를 만들었다 — iOS Safari를 비롯한 여러 모바일 브라우저는 `print()`/`alert()`처럼 대화상자를 여는 API를 사용자 제스처의 **동기 콜스택 안에서 호출했을 때만** 허용하고, `requestAnimationFrame`(또는 `setTimeout`)으로 한 틱이라도 늦추면 "사용자가 직접 트리거한 호출"로 간주하지 않아 조용히 무시해버린다. 그래서 이 지연은 되돌렸고, `window.print()`는 클릭 핸들러 안에서 클래스를 붙인 직후 바로(동기적으로) 호출한다 — 브라우저는 어차피 `print()` 호출 시점의 최신 DOM/스타일 상태로 인쇄 레이아웃을 계산하므로 별도 지연이 필요 없다.
  - **실제 원인 확정: 숨겨진 형제 요소가 만드는 "빈 뒤쪽 페이지"(2026-09)**: 사용자가 보내준 실제 인쇄 미리보기 스크린샷으로 확정됐다 — 예를 들어 `GaitDiagnosticsHistory`의 진단 기록 상세 하나만 인쇄해도 총 3페이지가 만들어지고 1페이지에만 실제 내용이 있고 2·3페이지는 완전히 빈 페이지였다. 원인은 `body * { visibility: hidden }` 기법의 잘 알려진 함정: `visibility: hidden`은 요소를 안 보이게만 할 뿐 문서 흐름에서 차지하는 공간(레이아웃 높이)은 그대로 남겨둔다. 인쇄 대상이 아닌 형제 요소들 — 방금 분석한 영상/차트가 있는 `.results` 섹션, 과거 기록이 길게 나열된 `.rom-history-list` 등 — 이 화면에서 매우 길면, 인쇄 대상 자체(`position:absolute`로 페이지 왼쪽 위에 고정)는 1페이지에만 그려지는데도 그 "보이지 않지만 여전히 차지하고 있는" 형제 요소들의 총 높이만큼 인쇄 엔진이 뒤에 완전히 빈 페이지를 추가로 만들어냈다. **해결**: `@media print`에 `body :not(.printable-report-active):not(.printable-report-active *):not(:has(.printable-report-active)) { display: none !important; }` 규칙을 추가했다 — 인쇄 대상 자신·그 자손·그 조상(대상을 그리는 데 필요하므로 `:has()`로 제외)을 뺀 모든 요소를 `display:none`으로 문서 흐름에서 완전히 제거해, 더 이상 빈 공간 자체가 생기지 않게 한다(모던 Chrome/Safari의 `:has()` 지원에 의존). 헤드리스 Chromium(Playwright)으로 정확히 이 시나리오(짧은 인쇄 대상 + 매우 긴 숨겨진 형제 요소들)를 재현해 수정 전 다중 빈 페이지 → 수정 후 정확히 1페이지로 줄어드는 것을 확인했고, 기존 재현 테스트(단순 1페이지, 강제 다중 페이지 스트레스 테스트)도 내용 누락 없이 계속 통과함을 재확인했다. 이전에 시도했던 `.printable-report-active`/자손에 대한 `height:auto`/`overflow:visible`/`transition:none`/`animation:none` 강제와 `break-inside: avoid` 보강은 근본 원인은 아니었지만 여전히 안전한 보강이라 그대로 유지한다.
- `src/shims/mediapipe-pose-shim.ts` — stub for `@mediapipe/pose`, which `@tensorflow-models/pose-detection` statically imports for its BlazePose runtime even though this app only uses MoveNet. Aliased in `vite.config.ts` so the real (non-ESM) package doesn't break the production build.
- `src/shims/mediapipe-hands-shim.ts` (2026-09-14) — same problem, same fix, for the hand/foot 기능이 추가한 `@tensorflow-models/hand-pose-detection`: 그 패키지가 MediaPipeHands "mediapipe" 런타임용으로 `@mediapipe/hands`에서 `Hands`를 정적 import하는데, 이 앱은 `runtime: 'tfjs'`만 쓰므로(`handDetector.ts`) 실제로는 그 클래스를 쓰지 않는다. 이 개발 샌드박스는 npm 레지스트리가 막혀 있어 미리 잡아낼 수 없었던 문제로, 실제 배포 VM에서 `docker compose build`가 `[MISSING_EXPORT] "Hands" is not exported by ".../@mediapipe/hands/hands.js"`로 처음 실패하고 나서야 발견됐다 — `@mediapipe/pose`와 똑같이 `vite.config.ts`에 별칭을 걸어 고쳤다.
- `src/shared/components/BulkSelectionBar.tsx` (2026-09-14) — 저장 기록 목록(ROM/보행진단/XMSK 마사지·평가/손·발, 총 6곳)이 전부 같은 "목록+상세+개별 삭제"(`.rom-history*` CSS 클래스, `handleDelete(id)`) 패턴을 쓰고 있어, 여러 개를 한 번에 선택해 삭제하는 기능도 6곳 전부에 동일하게 얹었다. 체크박스(전체 선택 포함)와 "선택 삭제" 버튼 자체는 도메인 타입에 의존하지 않는 순수 UI라 `shared/components`에 하나만 두고 6곳이 재사용한다(`ComparisonBarChart`/`PdfExportButton`과 같은 성격의 공유 — RangeSelector처럼 기능별 도메인 로직이 섞인 것과는 다르다). **새 벌크 삭제 API를 만들지 않았다**: XMSK 데이터 품질 대시보드처럼 이 개인용 앱 규모에서는 이미 있는 단건 삭제(`DELETE /:id`)를 선택된 개수만큼 순차 호출하는 것으로 충분하다고 보고, 각 히스토리 컴포넌트가 `Set<number>` 선택 상태와 `handleBulkDelete()`(순차 호출 → 성공한 것만 목록에서 제거, 실패한 항목은 선택 상태로 남겨 재시도 가능)를 자체적으로 관리한다. XMSK 두 화면(`XmskSessionHistory`/`XmskEvaluationHistory`)만 예외적으로 로직이 조금 더 있다 — 토큰이 중간에 만료되면(`XmskAuthError`) 그 시점까지 성공한 삭제만 반영하고 나머지는 중단한 뒤 잠금 화면으로 돌아간다(기존 단건 삭제의 인증 처리와 동일한 방식을 순차 루프에 그대로 적용).

**보행 분석** (`src/features/gait/GaitApp.tsx`):
- Owns the pipeline state machine (`AnalysisStage`: `idle → loading-model → processing → analyzing → done`/`error`). `SAMPLING_FPS` (12) and `MAX_ANALYSIS_SECONDS` (20) cap analysis cost.
- `src/features/gait/types.ts` — `StepEvent`, `GaitMetrics`, `FallRiskFactor`/`FallRiskAssessment`, `AnalysisStage` (imports `Side` from `shared/types/pose`).
- `src/features/gait/lib/gaitAnalysis.ts` — pure `computeGaitMetrics(frames)`: normalizes foot-y trajectories (heel when its per-clip confidence coverage is high enough, else ankle as a fallback; low-confidence frames — e.g. a foot briefly occluded by the other leg — hold the last good value instead of injecting a spurious jump) by an estimated leg-length scale, finds foot-strike peaks per side, derives cadence, step interval/length, left-right symmetry, and lateral sway. All distance metrics are leg-length-relative units, not meters, since camera distance/angle isn't controlled. Also computes `computeFallRisk()`: a 0–100 "전도(낙상) 위험 참고 점수" combining step-interval variability (CV%), temporal/step-length symmetry, sway-to-step-length ratio, and cadence — weighted from the direction (not a validated numeric cutoff) of recent gait/fall-risk research, explicitly presented as a reference indicator, not a clinical probability (no standardized cutoffs exist in the literature for these metrics, and the one concrete published fall-probability formula found needs clinical/metric inputs this camera-only, uncalibrated pipeline can't provide).
  - **왜 걸음걸이가 "몸만"의 문제가 아닌가 — SCAN 이론과의 연관(2026-09, 문서 보강만, 계산 로직 변경 없음)**: 2023년 Nature에 발표된 체성-인지 행동 네트워크(SCAN, Somato-Cognitive Action Network) 이론은 운동피질 안에 손·발 등 신체 부위별 정밀 제어 영역과, 전신 계획·자율신경 조절을 담당하는 별도의 통합 네트워크가 서로 얽혀 있다는 것을 fMRI로 규명했다(Gordon et al., "A somato-cognitive action network alternates with effector regions in motor cortex," Nature 617, 2023, doi:10.1038/s41586-023-05964-2). 이후 Michael J. Fox Foundation이 지원하는 후속 연구는 파킨슨병의 보행 장애를 바로 이 SCAN 네트워크의 이상 연결성과 직접 짝지어 측정하고 있다 — 뇌 영상뿐 아니라 **실제 보행 평가 자체를 핵심 지표로 삼는다**(약 복용 전/후 보행을 각각 측정해 SCAN 연결성 변화와 비교). 이는 이 앱의 걸음걸이 분석이 왜 단순 생체역학을 넘어 신경학적으로도 의미를 가질 수 있는지에 대한 추가적인 연구 맥락일 뿐 — `computeFallRisk()`의 계산식이나 가중치를 바꾸지는 않았다. 카메라 기반 보행 지표(케이던스, 좌우 대칭성 등)가 SCAN 연결성 자체를 측정하는 것은 전혀 아니라는 점은 분명히 해둔다.
  - **검토했지만 적용하지 않은 것 — Fastball(FPVS) EEG 뇌파 검사**: 치매(알츠하이머) 조기 발견용으로 주목받는 검사법으로, 초당 3장씩 깜빡이는 이미지에 대한 뇌의 무의식적 기억 반응을 EEG 캡으로 측정한다(Brain, 2021, doi 관련 Fastball memory assessment 논문; dementiacarecentral.com 설명). **이 앱에 적용할 방법이 없다** — 손가락/발가락처럼 "더 나은 카메라 모델을 찾으면 되는" 문제가 아니라, 애초에 측정 대상 자체(뇌파/EEG 전위)가 카메라로는 원리적으로 관측 불가능한 신호이기 때문이다. 카메라 기반 대체 지표(예: 반응시간, 시선)에 대한 언급도 없었다. 이 개인 프로젝트가 소비자용 EEG 헤드밴드 같은 새 하드웨어와 연동하기 전에는(완전히 다른 아키텍처가 필요한 별개의 확장) 이 검사법을 흉내조차 낼 수 없다는 것이 조사 결론이다.
- `src/features/gait/components/SkeletonViewer.tsx` — scrubbable playback with the detected skeleton (including feet) drawn on a `<canvas>` over the video.
- `src/features/gait/components/GaitMetricsPanel.tsx` — numeric summary of `GaitMetrics` plus the fall-risk reference panel (score bar, level badge, contributing factors, disclaimer). When `totalSteps < 2` it also renders `debugStats` (from `extractPoseFrames`'s optional `ExtractDebugStats` output — attempted/dropped/kept frame counts, per-keypoint average confidence, video resolution) so a poor-detection report can be diagnosed from what's on screen, without remote debugging.
- `src/features/gait/lib/gaitDiagnosticsApi.ts` / `components/GaitDiagnosticsHistory.tsx` — every gait analysis run (regardless of outcome) auto-saves its `ExtractDebugStats` + basic result summary (device `navigator.userAgent`, optional `clientName`/`trainerName` typed into `GaitApp`'s header inputs, video resolution/duration, frame counts, per-keypoint confidence, `sampledTimesChecksum`, step count/cadence) to `/api/gait-diagnostics`, so detection failures (and run-to-run drift) across different devices/browsers/clients can be compared later. `GaitDiagnosticsHistory` (rendered at the bottom of `GaitApp`) lists saved records with a debounced search box (matches video name / client / trainer / device info / note) and a per-row delete button — mirrors the `RomSessionHistory`/`XmskSessionHistory` list+detail+delete pattern and reuses their `.rom-history*` CSS classes. `GaitApp` shows a `회원 이름`(required)/`담당 트레이너`(optional) input pair — reusing XMSK's `.xmsk-eval-header-grid` styling — above the video uploader; the uploader itself is only rendered once a client name is entered, since gait analysis starts automatically on file selection (no separate "start" button to gate on).
- `src/features/gait/components/GaitCharts.tsx` — `StepIntervalChart` (time series; gait-only, `ComparisonBarChart`은 `shared/components`로 분리됨).

**스트레칭 가동범위 분석** (`src/features/rom/RomApp.tsx`):
- State machine (`RomAnalysisStage`: `idle → loading-model → marking-range → processing → analyzing → done`/`error`). After loading the video and pose model, the user marks a "before" and "after" time window on one timeline (`RangeSelector`), then `extractPoseFrames` runs once per window.
- Shows a `회원 이름`(required)/`담당 트레이너`(optional) input pair (`.xmsk-eval-header-grid`, same styling as XMSK) above the video uploader, mirroring XMSK's client/trainer header. Unlike XMSK (which gates its own explicit "기록 저장" button), ROM auto-saves right after analysis, so the client name requirement is enforced earlier — the "분석하기" button in the `marking-range` stage stays disabled until both the range lengths are valid *and* a client name is entered (`canAnalyze`).
- Because the session is auto-saved (`POST /api/rom-sessions`) the moment analysis finishes, but the 손목 저항 manual-input fields (see `xmskEstimates.ts` below) can only be typed in afterward, `RomApp` keeps the returned session id in a ref (`savedSessionIdRef`) and `handleManualInputChange` debounces a follow-up `updateRomSessionManualInputs()` call (`PUT .../manual-inputs`) 500ms after the trainer stops typing, so repeated keystrokes don't spam the API. `RomSessionHistory`'s saved-record view passes the same `manualInputs` into `RomResultsPanel` without `onManualInputChange`, so it renders read-only there.
- Before/after terminology matches XMSK's pain-recipe step names throughout the UI (`RangeSelector` handles/readout, `RomResultsPanel` table headers): "before" → `0.5 상태체크`, "after" → `3. 마무리` (XMSK's own before/after comparison-chart labels stay `이전`/`이후`, so ROM's chart legend does too, for consistency with that convention).
- `src/features/rom/types.ts` — `JointKey` (8 values: L/R shoulder/elbow/hip/knee), `RomJointResult`, `RomRange`, request/response types (`RomSessionCreateRequest`/`RomSessionListItem` include `clientName`/`trainerName`).
- `src/features/rom/lib/jointAngles.ts` — `computeJointAngles(frame)`: per-frame angle-at-vertex (atan2-based) for each of the 8 joints, using only COCO-17 keypoints MoveNet provides (no spine/toe angles). Null if any involved keypoint's score is below threshold.
- `src/features/rom/lib/romAnalysis.ts` — `computeRomSummary(beforeFrames, afterFrames)`: per joint, smooths the angle series (`movingAverage`) and takes min/max within each phase → `romDeg = max - min`; `deltaRomDeg = after - before`. Requires ≥3 valid samples per phase, else `null` ("데이터 부족").
- `src/features/rom/lib/xmskEstimates.ts` — `computeXmskEstimates(beforeFrames, afterFrames)`: reuses the *same* before/after pose frames as the 8-joint ROM table to additionally estimate whichever of XMSK's pain-recipe "0.5 상태체크"/"3. 마무리" `measurements` (across all 8 XMSK regions) can plausibly be derived from BlazePose body landmarks — a separate, additive section (`RomResultsPanel`'s XMSK 추정 항목 section), never replacing or altering the original 8-joint results. Of XMSK's ~13 distinct measurement fields: `armRaise`/`armReach` (shoulder), `hipFlexion`, `kneeFlexion` are skipped as redundant with the existing 8-joint ROM table (surfaced per-region as `XMSK_COVERED_BY_EXISTING_8` notes). The remaining 9 are computed: `neckRotation`/`neckSideBend` (approximated from ear/eye landmarks — BlazePose's raw `keypoints` array, not the named `PoseFrame` fields — since head yaw about the camera's vertical axis can't be read directly from a monocular 2D image, `neckRotation` uses an inter-ear-distance foreshortening ratio against this run's most-frontal frame as a "0°" reference), `legAbduction`/`dorsiflexion` (joint-angle ROM, same min/max-over-window method as the 8-joint table, just for hip-abduction and ankle-dorsiflexion geometry instead), `squat`/`pelvisThoracicSeparation` (0–10 scores mapped from, respectively, peak knee/hip flexion depth and peak shoulder-line-vs-hip-line rotation separation — the latter reusing sports biomechanics' golf-swing "X-Factor" concept), `thoracicExtRot` (the same shoulder/hip rotation-separation signal, reported as peak left/right degrees instead of a 0–10 score), `forwardBend` (no cm value is possible without camera calibration, so it's reported as a dimensionless wrist-to-ankle-height gap normalized by leg length — the same "leg-length-relative units" trick `gaitAnalysis.ts` uses for lateral sway), and `wristFlexExt` (손목 굴곡·신전 각도 — see correction below). Each result carries an `evidence` tag — `'approximate'` (a validation study exists for a closely related pose-estimation measurement — e.g. hip-abduction and ankle-dorsiflexion angle-vs-goniometer/mocap studies, or FMS-style pose-based squat scoring) or `'experimental'` (geometrically motivated but no validation study found — true of every rotation-about-the-vertical-axis metric: neck rotation, thoracic rotation, X-Factor-style pelvis-thoracic separation, plus the forward-bend leg-length normalization and wrist flex/ext) — and a `note` explaining its specific basis/caveat, both shown in the UI (`RomResultsPanel`'s evidence badges + a collapsible methodology `<details>`). As with `gaitAnalysis.ts`'s fall-risk score, none of this is a diagnostic measurement — it's explicitly framed as a reference/experimental estimate layered on top of a hobby project's existing non-diagnostic tool, grounded in real (cited in code comments) but not directly-validated-for-this-use research.

  **`wristFlexExt` correction (BlazePose does have hand-knuckle landmarks):** this item was originally excluded on the mistaken claim that BlazePose's 33-point body model has no finger/hand landmarks. It actually does — indices 17–22 are `left_pinky`/`right_pinky`, `left_index`/`right_index`, `left_thumb`/`right_thumb` (knuckle-region points; Bazarevsky et al., "BlazePose: On-device Real-time Body Pose Tracking," arXiv:2006.10204, 2020). `computeWristFlexExt()` averages the index/pinky knuckle points as a "hand direction" reference point and computes the elbow–wrist–hand angle's ROM the same way as the other joint-angle metrics, tagged `'experimental'` — hand-region landmarks are reported as less reliable than core body joints (a webcam-based ROM study excluded wrist flex/ext from its own measurement set for this reason: PLOS ONE, "A webcam-based machine learning approach for three-dimensional range of motion evaluation," 2023).

  **`wristResist` (도수근력검사) has no video-based substitute, so it's a manual input, not a computed estimate:** it's a resisted-strength test where the examiner applies manual resistance — nothing a camera can measure. Telehealth literature (Alrwaily, "Delivering Musculoskeletal Rehabilitation in the Digital Era," Healthcare 13(18):2286, 2025) confirms this is handled in practice by having a caregiver apply resistance while the clinician observes remotely — i.e. there is no validated camera-only substitute, so this app doesn't fabricate one either. Instead, `XMSK_MANUAL_ITEMS` defines a pair of 0–10 trainer-entered fields (`wristResist_left`/`_right`) that `RomResultsPanel` renders as editable number inputs (via `ManualInputRow`) alongside the computed rows, persisted the same way as computed estimates.

  **Region-grouped display:** `groupEstimatesByRegion()` groups computed rows, "covered by existing 8" notes, and manual-input rows under XMSK's own 8 pain-recipe region titles (목·어깨/허리/무릎/발목/고관절/팔꿈치/손목/등 통증), so `RomResultsPanel` renders one heading per region with everything relevant to it underneath (a "골반-흉추 분리" measurement shared by two regions in XMSK's own recipe data appears under both). This duplicates XMSK's region→measurement mapping locally (`XMSK_REGIONS`) rather than importing from `src/features/xmsk/`, keeping HomeScreen→rom the only intentional cross-feature reference in the codebase (existing architectural rule, unchanged).
- `src/features/rom/lib/romApi.ts` — fetch wrappers for `/api/rom-sessions` (create/list/get/**delete**), plus `updateRomSessionManualInputs(id, xmskManualInputs)` (`PUT /api/rom-sessions/:id/manual-inputs`) — a separate call because manual-input fields (e.g. 손목 저항) are typed in by the trainer *after* the analysis result is already auto-saved (see `RomApp.tsx` below), so they can't ride along in the original `POST`; also imported by `HomeScreen` for the dashboard preview.
- `src/features/rom/components/RangeSelector.tsx` — dependency-free dual-range timeline (4 draggable handles via Pointer Events) for marking the before/after windows; syncs the preview video's `currentTime` while dragging.
- `src/features/rom/components/RomResultsPanel.tsx` — per-joint before/after/delta table + reused `ComparisonBarChart`, plus (when present) an XMSK 추정 항목 section grouped by `groupEstimatesByRegion()` into the 8 pain-recipe region headings (`<h5>`), each with a combined table of computed rows (evidence badges) and manual-input rows (`ManualInputRow` — editable 0–10 number inputs when `onManualInputChange` is passed, else read-only text), plus per-region "covered by existing 8" and manual-item notes, and a bottom methodology `<details>`. `RomApp` (fresh analysis) passes `onManualInputChange` so the trainer can type values in; `RomSessionHistory` (saved record detail) omits it, rendering the same manual rows read-only.
- `src/features/rom/components/RomSessionHistory.tsx` — lists saved sessions (with client/trainer name when set) from the backend, click to view detail, per-row delete button with `window.confirm()` (mirrors `GaitDiagnosticsHistory`/`XmskSessionHistory`). Also owns the debounced save for trainer ground-truth edits made in the saved-record detail view (see below) — a separate timer/handler from `RomApp`'s, since the two screens are different React trees hitting the same server record.

**트레이너 실측값(ground truth) 입력 — AI 추정 보정용 학습 데이터 수집 (2026-09)**: `docs/ai-training-plan.md`에서 정리한 "AI 훈련·추론데이터 확보" 계획의 첫 실행 항목. `RomXmskGroundTruth`(`estimateId`, `verifiedValue`)는 `RomXmskManualInput`(영상으로 아예 측정할 수 없는 항목 전용, 예: 손목 저항)과는 목적이 다르다 — 이건 **AI가 이미 계산한 추정치(`RomXmskEstimateResult`)가 있는 모든 항목**에 대해, 트레이너가 실제로 관찰·측정한 값을 나란히 기록해 "AI 추정이 실제와 얼마나 맞았는지"를 나중에 확인하기 위한 것이다. `RomResultsPanel`은 XMSK 추정 테이블에 "트레이너 실측값" 컬럼을 추가로 렌더링하고(`GroundTruthCell`, `onGroundTruthChange`가 있을 때만 입력 가능), 수기 입력 전용 행(`ManualInputRow`)에는 대응하는 AI 추정치가 없으므로 이 컬럼에 `—`만 표시한다. 지금 당장은 이 값이 어떤 계산도 바꾸지 않는다 — 오직 짝지어 쌓아두는 것이 목적이다.

수기 입력(`xmskManualInputs`)과 달리 ground truth는 **분석 직후 화면(`RomApp`)뿐 아니라 저장된 기록 상세 화면(`RomSessionHistory`)에서도 편집 가능**하다 — 트레이너가 실제로 관찰할 시간이 필요해서 분석 당일이 아니라 나중에 값을 채워 넣는 경우가 많기 때문에, 기존 manual-input의 "분석 직후에만 편집 가능" 제약을 그대로 따르지 않기로 한 의도적인 차이다. 두 화면 모두 각자 디바운스 타이머로 `PUT /api/rom-sessions/:id/ground-truth`를 호출해 같은 서버 레코드를 갱신한다.

보행(gait)에도 같은 개념을 적용했다: `GaitGroundTruth`(`id`, `verifiedValue`)와 `GAIT_VERIFIABLE_ITEMS`(`src/features/gait/lib/gaitGroundTruth.ts` — 케이던스, 좌우 걸음 간격 대칭성, 좌우 보폭 대칭성, 전도 위험 점수의 4개 항목, 각각 `GaitMetrics`에서 AI 추정값을 뽑는 getter 포함)를 새 컴포넌트 `GaitGroundTruthPanel`이 표로 렌더링한다. `GaitApp`(분석 직후 화면)과 `GaitDiagnosticsHistory`의 `RecordDetail`(저장된 기록 조회 화면) 양쪽에서 편집 가능하며, `PUT /api/gait-diagnostics/:id/ground-truth`로 저장한다 — ROM과 동일한 이유(트레이너가 나중에 관찰한 값을 입력)로 두 화면 모두 편집 가능하게 했다. `GaitApp`은 분석 직후 자동 저장(`saveGaitDiagnostic`)이 반환하는 `id`를 이제 `savedDiagnosticIdRef`에 저장해두는데, 이전에는 이 id를 버리고 있었다(ground truth를 갱신하려면 어떤 레코드인지 알아야 하므로 이번에 추가됨).

**결과-버전 태깅 (`ANALYSIS_PIPELINE_VERSION` / `calcVersion`, 2026-09)**: `docs/ai-training-plan.md` 로드맵의 다음 항목. 트레이너 실측값을 나중에 대량으로 모아 AI 추정 계산 방식을 개선하려면, 그 실측값이 "어떤 버전의 계산 로직이 만든 AI 추정치와 짝지어졌는지"를 반드시 구분할 수 있어야 한다 — 그렇지 않으면 포즈 모델, 샘플링 방식, One Euro Filter 파라미터, gait/rom 추정 공식(`gaitAnalysis.ts`, `xmskEstimates.ts`) 등이 바뀌기 전/후의 데이터가 뒤섞여 보정 학습 자체가 무의미해진다. 이를 위해 `src/shared/lib/poseDetector.ts`에 문자열 상수 `ANALYSIS_PIPELINE_VERSION`(현재 `'2026-09-13'`)을 하나 두고, 위에 나열한 계산 로직 중 하나라도 바뀔 때마다 사람이 직접 값을 올려야 한다(자동 감지 아님 — 코드 주석에 무엇이 이 값을 올려야 하는 변경인지 설명해뒀다). `RomApp`/`GaitApp`은 세션/진단 기록을 저장할 때(`POST`) 이 값을 그대로 `calcVersion` 필드에 실어 보내고, 서버는 다른 `*_json` 컬럼들과 같은 방식으로 `calc_version` 컬럼에 검사 없이 그대로 저장/반환만 한다(`server/db.ts`의 `ensureColumn`으로 추가 — 이 컬럼이 생기기 전 기존 기록은 `null`). 화면에는 값이 있을 때만 작게 표시된다 — ROM은 `RomResultsPanel`의 방법론 `<details>` 안 목록 끝에 "계산 버전: …"으로, 보행은 `GaitDiagnosticsHistory`의 진단 기록 상세 `debug-stats-list`에 같은 형식으로 추가했다(방금 분석한 `GaitApp` 결과 화면에는 늘 최신 버전이므로 표시하지 않음). 이 값 자체는 계산에 전혀 관여하지 않는다 — 오직 나중에 실측값-AI추정치 쌍을 모을 때 버전이 다른 데이터를 섞지 않기 위한 꼬리표다.

**손·발 분석 (hand/foot analysis)** (`src/features/handfoot/HandFootApp.tsx`, 2026-09 추가): 보행/ROM/XMSK 이후 새로 추가된 네 번째 기능. 손가락 14마디(관절 각도)와 발가락 10마디(마디 개수) 요청에서 출발했고, 조사 결과 손과 발은 근본적으로 다른 기술적 현실이라 하위 모듈(`손 분석`/`발 분석`, XMSK의 모듈 탭 전환과 같은 패턴)로 나눴다.

- **왜 손·발 움직임을 따로 보나(연구 근거)** — 자세한 인용은 `HandFootApp.tsx` 상단 주석 참고. 손 쪽은 악력(grip strength)이 노년의학에서 전신 건강·사망률의 "활력징후"로 제안될 만큼 검증된 지표라는 폭넓은 연구(J Health Popul Nutr narrative review, 2024; PURE 코호트 계열)와, 손가락 세밀 운동(파킨슨병 손가락 두드리기 검사)을 영상만으로 정량화하는 컴퓨터 비전 연구(npj Parkinson's Disease, 2026)가 있다 — 다만 악력(쥐는 힘)과 이 기능이 재는 관절 가동범위는 다른 개념이라는 차이를 명확히 안내한다. 발 쪽은 발가락 굽힘근(toe flexor) 근력이 노인 균형·낙상 위험과 관련 있다는 연구(Mickle et al., Clin Biomech 2009 "ISB Clinical Biomechanics Award"; 토 flexor 강화-균형 체계적 문헌고찰 계열)가 있으나, 이 연구들은 "근력"을 압력판·동력계로 측정하지 카메라 영상의 "가동범위"로 측정하지 않는다 — 방향은 같지만(발가락 기능과 하체 안정성의 연관) 같은 지표는 아니라는 한계를 UI와 코드 주석에 명시했다.
- **손 — 왜 별도 모델이 필요한가**: 기존 BlazePose(가동범위/보행에 쓰는 몸 전체 포즈 모델)는 손목과 손가락 관련 랜드마크를 지금(`left_pinky`/`index`/`thumb` 등, ROM의 wristFlexExt가 쓰는) 손가락당 점 하나씩만 제공해 관절별 각도를 계산할 수 없다. 손가락 관절 각도 14개(네 손가락 각 3관절 MCP·PIP·DIP + 엄지 2관절 MCP·IP)를 계산하려면 손가락마다 여러 랜드마크가 필요한 전용 손 모델이 필요하다 — `@tensorflow-models/hand-pose-detection`(MediaPipeHands, 21랜드마크)을 새 의존성으로 추가했다(`shared/lib/handDetector.ts`). **⚠️ 이 패키지는 이 개발 샌드박스의 막힌 npm 레지스트리 때문에 실제 설치·타입 검증 없이 작성됐다**(PdfExportButton 관련 기존 제약과 같은 이유) — API 형태(`SupportedModels.MediaPipeHands`, `createDetector`, `estimateHands`, 21개 랜드마크 이름)는 이 패키지가 수년간 유지해온 공개 인터페이스를 기준으로 작성했다.
  - **실제로 터진 문제(2026-09-14) — `npm ci`가 lock 파일 불일치로 빌드 자체를 실패시킴**: 예상대로 배포 시 문제가 생겼는데, 타입 불일치가 아니라 그 이전 단계였다 — `package.json`에 새 의존성(`@tensorflow-models/hand-pose-detection`과 그 하위 의존성 `@mediapipe/hands`, `tslib`)을 추가했지만 `package-lock.json`은 이 개발 샌드박스에서 실제로 설치해보며 갱신할 방법이 없어 예전 그대로였다. `Dockerfile`의 `RUN npm ci`는 두 파일이 정확히 일치해야만 동작하므로 "Missing ... from lock file" 오류로 빌드 이미지 생성 자체가 실패했다(직전 배포가 살아있었으므로 사이트 다운타임은 없었다). **고침**: 빌드 VM은 실제 인터넷에 접근할 수 있으므로, `Dockerfile`의 두 `RUN npm ci`를 전부 `RUN npm install`로 바꿨다 — lock 파일이 완전히 일치하지 않아도 실제 npm 레지스트리에서 새 패키지를 그대로 받아 설치한다(재현성은 `npm ci`보다 약간 떨어지지만, lock 파일을 사전 검증할 수 없는 이 개발 흐름에서는 실용적인 선택). 이 변경 이후 `npm run build`(`tsc -b`)가 실제로 도는 시점에 타입 불일치가 있다면 그때 비로소 드러날 것이다 — 그러면 오류 메시지를 그대로 전달해 고치면 된다(poseDetector-build-fix 사례와 같은 성격).
  - **바로 다음 배포에서 두 번째 문제 발생 — 타입이 아니라 번들러 ESM 문제**: `npm install`로 lock 문제를 넘기고 나니 예상대로 그 다음 단계에서 진짜 문제가 나왔는데, 타입 불일치가 아니라 `vite build`(rolldown 번들러) 단계에서 `[MISSING_EXPORT] "Hands" is not exported by "node_modules/@mediapipe/hands/hands.js"`였다. 원인은 이 프로젝트에 이미 있던 문제와 완전히 같다 — `@tensorflow-models/hand-pose-detection`이 MediaPipeHands의 "mediapipe" 런타임을 지원하려고 `@mediapipe/hands`에서 `Hands`를 정적 import하는데, 이 패키지는 진짜 ESM이 아니라 브라우저에 바로 넣는 스크립트라 번들러가 이름 있는 export를 찾지 못한다(`@tensorflow-models/pose-detection`이 `@mediapipe/pose`에 대해 이미 겪었던 것과 같은 문제 — `mediapipe-pose-shim.ts` 참고). 이 앱은 애초에 `runtime: 'tfjs'`만 쓰므로(`handDetector.ts`) 그 mediapipe 런타임 자체가 필요 없다. **고침**: 같은 패턴 그대로 `src/shims/mediapipe-hands-shim.ts`(던지기만 하는 `Hands` 스텁 클래스)를 만들고 `vite.config.ts`에 `@mediapipe/hands` → 이 스텁으로의 별칭을 추가했다. 이런 종류의 "타입은 맞는데 번들러가 실제 패키지 구조를 못 받아들이는" 문제는 이 개발 샌드박스에서 `npm install`을 못 해봐서 애초에 예측할 수 없는 범주였다 — 앞으로 이 종류의 새 의존성을 추가할 때는 "타입 불일치"뿐 아니라 "이 패키지가 mediapipe 런타임처럼 실제로 안 쓰는 하위 mediapipe 패키지를 정적 import하지 않는지"도 배포 후 첫 빌드에서 확인해야 한다는 교훈을 남긴다.
- **손 — 프레임 추출은 왜 훨씬 단순한가**: `poseDetector.ts`의 재생 기반(`requestVideoFrameCallback`) 정밀 추출 엔진은 다섯 단계의 실전 버그 수정을 거친 코드라(위 설명 참고), 이걸 손 모델용으로 일반화하다가 이미 안정적인 보행/ROM에 회귀를 낼 위험을 피하기 위해 `handDetector.ts`의 `extractHandFrames`는 훨씬 단순한 seek 반복 방식만 쓴다. 손 동작은 트레이너가 지정한 짧은 구간 동안 비교적 정적인 자세(쥐기/펴기 등)를 담는다는 전제라, 프레임 정확도가 다소 떨어져도 크게 문제되지 않는다는 판단이다.
- **손 — 관절 각도 계산**: `handAngles.ts`의 `computeHandJointAngles()`는 `jointAngles.ts`(ROM)와 같은 atan2 기반 정점각 공식을 21랜드마크에 적용한다 — 엄지는 wrist-thumbMcp-thumbIp(MCP), thumbMcp-thumbIp-thumbTip(IP), 나머지 네 손가락은 각각 wrist-MCP-PIP, MCP-PIP-DIP, PIP-DIP-TIP 세 각도. `handAnalysis.ts`의 `computeHandRomSummary()`는 ROM의 `computeRomSummary()`와 동일하게 구간 내 최소~최대 각도를 그 구간의 "가동범위"로 본다. 좌/우 손 구분은 모델의 handedness 판정을 그대로 쓰므로 촬영 방향(전면/후면 카메라)에 따라 반대로 나올 수 있다는 점을 UI에 안내했다.
- **발 — 개별 발가락을 인식할 수 없다는 한계, 그리고 실험적 절충안**: 조사 결과 카메라만으로 발가락 하나하나의 관절을 구분해 인식하는 검증된 모델은 현재 존재하지 않는다(보행/무릎/발뒤꿈치 수준 마커리스 포즈 추정 연구는 있지만, 손처럼 손가락마다 랜드마크를 주는 모델이 발에는 없다 — BlazePose도 발마다 heel·foot_index 두 점뿐). 이 제약을 사용자에게 명시적으로 알리고 두 가지로 절충했다: ① `footToeEstimate.ts`의 `computeFootToeEstimates()`가 발목-발끝(foot_index)-뒤꿈치 각도로 "발 전체"를 대표하는 실험적 근사치(`evidence: 'experimental'`) 하나만 좌/우 각각 계산한다(ROM의 XMSK 추정 항목과 같은 근거 표기 관례). ② 실제 "발가락 10마디"(발 한쪽 기준 엄지~새끼 각 MTP·IP)는 `handfootManualItems.ts`의 `FOOT_MANUAL_TOE_ITEMS`(양발 20개)로 정의한다. 이 수기 입력은 분석 직후 화면과 저장 기록 조회 화면 양쪽에서 계속 채워 넣을 수 있다(`PUT /api/foot-sessions/:id/manual-toe-inputs`) — ROM의 ground truth와 같은 이유(트레이너가 시간을 두고 관찰).

- **발가락 마디별 시뮬레이션 — 손가락 14마디 연구 과정을 발에 적용 (2026-09-13 업그레이드)**: 손가락은 실제 랜드마크(MediaPipeHands 21점)로 관절마다 독립 측정이 가능했지만, 발가락은 그런 모델 자체가 없어 처음엔 20개 항목 전부 순수 수기 입력으로만 뒀다. 이후 "손가락 분석에 쓴 연구 조사 방식을 발가락에도 적용해 분석툴을 업그레이드해달라"는 요청에 따라, 발 전체 실험적 신호 하나(`computeFootToeEstimates`의 결과)를 부위별 연구 근거로 배분하는 **시뮬레이션 모델**을 추가했다(`footToeEstimate.ts`의 `computeFootToeSegmentEstimates()`) — 20개 항목 각각에 "AI 시뮬레이션 초기값"을 자동으로 채워주고, 트레이너가 확인/수정하는 흐름으로 바뀌었다(사용자가 명시적으로 선택한 통합 방식 — 대안으로 "수기 입력 옆에 병기"/"문서만 보강"도 제시했으나, 매 항목을 트레이너가 직접 처음부터 입력하지 않아도 되게 하는 이 방식을 선택함).
  - **근거로 쓴 연구(방향성만 차용, 숫자를 그대로 옮긴 것이 아님)**: 엄지 MTP 관절의 정상 가동범위(수동 신전 40–100°, 보행에 필요한 기능적 신전 45–60° — QUT Podiatry Anatomy App "1st MTP Joint ROM"; Physiopedia "Hallux Rigidus"), 엄지 MTP가 제한되면 엄지 IP가 과신전으로 "보상"하는 역상관 패턴(Physiopedia "Hallux Rigidus"), 2~5번 발가락 MTP 관절이 엄지보다 더 제한된 가동범위를 보인다는 연구("Limited range of motion of the lesser MTP joints — a cause of metatarsalgia," Foot Ankle Surg 2004), 그리고 "엄지의 힘 생성이 나머지 발가락보다 발목 각도 변화에 더 강하게 좌우된다"는 연구(Kurihara et al., "Force Generation on the Hallux Is More Affected by the Ankle Joint Angle than the Lesser Toes: An In Vivo Human Study," Biology 10(1):48, 2021, doi:10.3390/biology10010048). **이 연구들은 전부 힘(force)이나 임상적으로 측정한 가동범위를 다루지, 이 앱이 계산하는 "카메라 기반 굽힘 각도"를 다루지 않는다** — 그래서 숫자를 그대로 인용하지 않고 "엄지가 발목과 가장 강하게 연동되고 가장 큰 가동범위를 갖는다 / 2~5번은 더 제한적이고 서로 묶여 움직인다 / 엄지 IP는 MTP보다 작고 관계가 더 복잡하다"는 **방향**만 가져와 `TOE_SEGMENT_SIM_PARAMS`(`referenceMaxDeg` 참고 상한 + `coupling` 결합계수)라는 비례 배분 모델을 만들었다.
  - **계산 방식**: 발 전체 신호(발목-발끝-뒤꿈치 각도)의 전/후 두 값만으로 "이 세션 안에서 상대적으로 얼마나 굽혀졌는가"(`curlFraction`, 0~1)를 자기 상대적으로 정규화하고(다른 세션과 비교할 수 있는 절대 기준이 아님), 여기에 부위별 `referenceMaxDeg × coupling`을 곱해 항목별 추정 각도를 만든다. 발 전체 신호 자체를 못 구한 경우(랜드마크 신뢰도 부족)엔 그 발의 20개 항목 전부 `null`로 남겨 순수 수기 입력으로 되돌아간다.
  - **정직성 장치 — `FootManualToeInput.source`**: 자동 채워진 값은 `source: 'estimated'`(UI에 "AI 시뮬레이션(미확인)" 배지)로 표시되고, 트레이너가 값을 한 글자라도 고치면 그 항목만 `source: 'trainer'`("트레이너 확인" 배지)로 바뀐다 — 손가락처럼 실제로 관절을 인식한 값이 절대 아니라는 점을 데이터 자체에 남겨, 나중에 "AI 시뮬레이션 vs 트레이너 실측"을 구분해서 집계할 수 있는 토대를 마련했다(아직 실제 집계/비교 화면은 만들지 않음 — 아래 "아직 안 한 것" 참고).
  - **버전 태깅**: 발 분석의 `calcVersion`은 이제 몸 전체 포즈 파이프라인 버전과 이 시뮬레이션 배분식 버전을 합친 문자열이다 — `${ANALYSIS_PIPELINE_VERSION}+toeSim:${FOOT_TOE_SIM_VERSION}`(`FOOT_TOE_SIM_VERSION`은 `footToeEstimate.ts`에 정의, 현재 `'2026-09-13'`). 두 계산 단계가 서로 독립적으로 바뀔 수 있어서(포즈 추출 방식이 바뀌는 것과, 배분 비율/참고 상한이 바뀌는 것은 별개) 단일 문자열보다 합성 문자열로 기록해야 나중에 실측값을 정확한 계산 버전과 짝지을 수 있다.
- **저장/기록**: 손과 발은 이 프로젝트의 "기능별 테이블 하나" 관례를 따라 별도 테이블(`hand_sessions`, `foot_sessions`)과 별도 라우터(`server/routes/handSessions.ts`, `server/routes/footSessions.ts`, `/api/hand-sessions`, `/api/foot-sessions`)를 쓴다 — ROM/보행처럼 단일 kind로 묶는 다형(polymorphic) 테이블 설계는 쓰지 않았다(기존 관례 유지). `HandSessionHistory`/`FootSessionHistory`는 ROM의 `RomSessionHistory`와 같은 목록+상세+삭제 패턴이다. 두 기능 모두 결과 계산 파이프라인 버전을 저장한다 — 손은 새 `HAND_ANALYSIS_PIPELINE_VERSION`(handDetector.ts, 손 모델이 완전히 별개이므로 몸 전체 포즈용 `ANALYSIS_PIPELINE_VERSION`과 독립), 발은 기존 몸 전체 포즈 파이프라인을 재사용하므로 `ANALYSIS_PIPELINE_VERSION`을 그대로 쓴다.
- **RangeSelector 중복**: `src/features/handfoot/components/RangeSelector.tsx`는 ROM의 `RangeSelector.tsx`와 거의 동일하지만(라벨만 props로 받도록 일반화) 별도로 복제했다 — 교차 기능 참조를 피하는 기존 관례(HomeScreen→rom만 예외) 때문. 마찬가지로 `angleAtVertexDeg` 순수 함수도 `handAngles.ts`/`footToeEstimate.ts`에 각각 작게 복제했다(원본은 `rom/lib/jointAngles.ts`).
- **아직 안 한 것**: 손·발은 아직 데이터 품질 대시보드 집계, `calcVersion` UI 표시 대상에는 포함되지 않았다(ROM/보행에만 적용된 상태) — 같은 패턴을 나중에 그대로 확장할 수 있다. 발가락 마디별 시뮬레이션(위 항목)이 `FootManualToeInput.source`로 "AI 시뮬레이션 vs 트레이너 확인"을 구분해두긴 했지만, ROM/보행의 ground-truth 패턴처럼 "AI 추정이 실제와 얼마나 맞았는지"를 집계하는 화면은 아직 없다 — trainer가 값을 수정한 양(추정치 대비 delta)을 나중에 모아 결합계수(`TOE_SEGMENT_SIM_PARAMS`)를 보정하는 데 쓸 수 있다는 것이 이 필드를 미리 넣어둔 의도다. 손가락 14마디는 이미 실제 랜드마크로 측정하므로 이런 시뮬레이션이 필요 없다(발과 다른 이유로 "아직 안 한 것"에 남아있는 것뿐).

**손·발 분석 × 동시 측정 뇌파(EEG) 참고 지표 (2026-09)**: 사용자가 "뇌파(EEG) 데이터를 분석해서 손발분석결과를 해석하는 최신연구자료를 검색하고 적용한 후 결과표를 제시하고 기록을 저장해 달라"고 명시적으로 요청했다 — 뇌파 기능이 원래 "서버에 아무것도 저장하지 않는 100% 브라우저 전용"으로 설계된 이유(위 뇌파 절 참고)와 정면으로 배치되는 요청이지만, 이번엔 사용자가 직접 저장을 요청했으므로 손·발 세션에 한해 예외적으로 저장한다.

- **먼저 실제 최신 연구를 조사했고, 그 결과가 "무엇을 만들지"를 그대로 결정했다**: "어느 손/발이 더 잘 움직였는가"를 EEG로 해석하는 기능을 만들어 달라는 요청으로 읽힐 수 있었지만, 조사 결과 이건 Muse 헤드밴드로는 과학적으로 뒷받침되지 않는다는 게 드러났다 — 운동 관련 좌우 비대칭을 EEG로 구분하려면 전운동/보조운동 영역 전극(FC5/FC6)이 필요하다는 것이 확인됐고("Using Mobile EEG to Investigate Alpha and Beta Asymmetries During Hand and Foot Use," Frontiers in Neuroscience, 2020, doi:10.3389/fnins.2020.00109; 후속 대규모 연구로 Mundorf et al., Frontiers in Human Neuroscience, 2026도 전두엽만으로는 운동 실행과 인지 처리를 구분하기 어렵다고 확인), Muse의 실제 전극(TP9·AF7·AF8·TP10)은 이 자리를 전혀 포함하지 않는다. Muse를 일반 인지 부하 지표로 쓰는 것도 별도 검증 연구에서 신뢰도가 낮았다("Reliability of MUSE 2 and Tobii Pro Nano at capturing mobile application users' real-time cognitive workload changes," Frontiers in Neuroscience, 2022, doi:10.3389/fnins.2022.1011475 — 12개 측정값 중 2개만 유의미). 카메라로 Fastball EEG 치매 검사를 흉내낼 수 없다고 결론 낸 것(보행 분석 절 참고)과 같은 종류의 원리적 하드웨어 한계라, 이런 판정은 절대 만들지 않기로 했다.
- **대신 실제로 검증된 두 지표만 채택**: Muse의 AF7/AF8(전두엽) 채널로 재는 전두엽 알파 비대칭(FAA, Frontal Alpha Asymmetry)은 동일 기준전극(귀 뒤 유양돌기 — Muse의 TP9/TP10과 같은 자리) 조건에서 연구용 장비와 r=.67의 상관을 보인 검증 연구가 있다("Validating the wearable MUSE headset for EEG spectral analysis and Frontal Alpha Asymmetry," bioRxiv, 2021, doi:10.1101/2021.11.02.466989) — 접근/회피 동기·정서가(valence)의 오래된 지표(Davidson의 접근-회피 모델 계열)다. 여기에 알파/(알파+베타) 비율(이완도 — Muse 제조사 자체의 명상 피드백 기능 설계 근거이자, 신체적 긴장이 미세운동 정밀도에 영향을 준다는 연구와도 방향이 맞는 지표)을 더해 두 개의 "동시 측정 참고 지표"만 계산한다. 둘 다 "이 손/발이 움직였다"가 아니라 "이 순간 신체적·정서적으로 얼마나 편안/긴장했는가"에 가까운 신호라는 점을 UI·주석에 항상 명시한다. 계산·연구 근거 전문은 `src/shared/lib/eeg/eegInterpretation.ts` 상단 주석 참고.
- **아키텍처: Muse 연결 코드를 `shared/lib/eeg/`로 이동**: 이 기능은 손·발 분석(`handfoot`)에서 Muse 연결/버퍼/FFT/에러 문구 코드가 필요한데, 이 프로젝트는 기능끼리 서로의 `lib`/`components`를 import하지 않는 관례(예외는 `HomeScreen→rom` 하나)라 `eeg` 기능의 코드를 그대로 가져다 쓸 수 없었다. `poseDetector.ts`가 여러 기능에서 재사용되는 것과 같은 이유로, `src/features/eeg/lib/{eegBuffer,museConnection,fft,errorMessages}.ts`와 `types.ts`를 `src/shared/lib/eeg/`로 옮기고 `eeg` 기능은 새 경로를 import하도록만 고쳤다(동작 변경 없음). 새 계산 파일 `src/shared/lib/eeg/eegInterpretation.ts`(`summarizeEegWindow`/`computeEegInterpretation`)도 이 shared 위치에 둔다.
- **캡처 UX — 영상과 동기화되지 않는다는 전제**: `src/features/handfoot/components/EegCaptureControl.tsx`는 영상 업로드/분석과는 별개로, 트레이너가 Muse를 씌운 채 ① 분석 시작 전(안정)과 ② 분석 직후(활동) 두 시점에 버튼을 눌러 각각 스냅샷을 캡처한다 — 영상은 미리 촬영해 업로드하는 것이라 촬영 당시의 뇌파를 나중에 재구성할 수 없으므로, "이 방문(세션) 동안"의 전/후 비교로 범위를 명확히 좁혔다. Web Bluetooth 연결은 `EegApp.handleConnect`와 같은 제약(클릭 핸들러의 동기 호출 스택 안에서 시작)을 그대로 따른다. Muse 연결 자체는 선택 사항이라, 연결하지 않으면 두 필드 모두 `null`로 남고 `EegContextPanel`은 아무것도 렌더링하지 않는다.
- **저장 — hand/foot 세션에 선택적 컬럼으로**: `HandSessionCreateRequest`/`FootSessionCreateRequest`에 선택 필드 `eegContext`(`EegHandFootContext`)를 추가했고, 서버는 다른 `*_json` 컬럼과 같은 방식으로 `hand_sessions.eeg_context_json`/`foot_sessions.eeg_context_json`에 검사 없이(얕은 객체 타입 검사만) 그대로 저장/반환한다(`ensureColumn`으로 추가 — 기존 행은 NULL). "② 활동" 캡처는 분석 완료(자동 POST 저장) 이후에 이뤄지는 경우가 많으므로, ROM의 manual-inputs/발의 manual-toe-inputs와 같은 이유로 별도 `PUT /api/hand-sessions/:id/eeg-context` / `PUT /api/foot-sessions/:id/eeg-context` 엔드포인트를 뒀다 — `HandAnalysisPanel`/`FootAnalysisPanel`의 `handleEegContextChange`가 세션이 아직 저장 전이면 state만 갱신해 다음 POST에 실어 보내고, 이미 저장된 뒤라면 이 PUT을 바로 호출한다.
- **결과표**: `src/features/handfoot/components/EegContextPanel.tsx`가 `computeEegInterpretation()`의 두 지표(전두엽 알파 비대칭 변화·이완도 변화)를 ROM의 XMSK 추정 항목과 같은 스타일(`rom-xmsk-table`, `evidence` 배지, `<details class="rom-xmsk-methodology">` 방법론 접기)로 보여준다 — `HandResultsPanel`/`FootResultsPanel` 안에 포함돼 있어 PDF 인쇄(`PdfExportButton`)에도 그대로 함께 나오고, `HandSessionHistory`/`FootSessionHistory`의 저장 기록 상세 화면에서도 동일하게 보인다.

**발가락 마디별 시뮬레이션 — "AI 추정 vs 트레이너 실측" 보정 데이터 파이프라인 (2026-09)**: "지금처럼 MyDoctor를 계속 고도화하면서, 나중에 실측값이 충분히 쌓이면 발가락 마디별 시뮬레이션 계수 같은 특정 계산 로직 하나를 작은 모델로 파인튜닝해볼 수 있지 않겠냐"는 사용자의 방향 제안에 대한 구체적인 첫 실행 단계. `docs/ai-training-plan.md`의 "옵션 B: 경량 보정 모델" 전략이 실제로 착수 가능한 시점을 스스로 판단할 수 있으려면, "AI가 처음에 뭐라고 추정했는지"와 "트레이너가 실제로 확인/수정한 값"을 잃지 않고 짝지어 쌓아야 하는데, 지금까지는 트레이너가 값을 고치는 순간 원래 AI 추정값이 덮어써져 사라지고 있었다(`source`만 `'trainer'`로 바뀔 뿐 원본은 남지 않음) — 이걸 고쳤다.

- **`FootManualToeInput`에 `estimatedBeforeValue`/`estimatedAfterValue` 필드 추가** — `computeFootToeSegmentEstimates()`(`footToeEstimate.ts`)가 20개 항목의 "AI 시뮬레이션 초기값"을 처음 채울 때 이 두 필드에 그 값을 그대로 복사해 영구 보존한다. 트레이너가 나중에 `beforeValue`/`afterValue`를 직접 고쳐도(그리고 `source`가 `'trainer'`로 바뀌어도) `FootAnalysisPanel.tsx`/`FootSessionHistory.tsx`의 `handleManualToeInputChange`가 원래부터 `{ ...m, ... }`로 기존 항목을 스프레드해서 갱신하는 구조였기 때문에, 코드를 더 바꾸지 않고도 이 두 필드가 자동으로 보존된다 — 순전히 타입/계산 함수 쪽의 변경만으로 해결됐다.
- **UI 피드백** — `FootResultsPanel.tsx`에 `CorrectionHint`를 추가해, 트레이너가 값을 고친 항목(`source === 'trainer'`) 옆에 "AI: 12.3°, +2.1" 같은 문구를 바로 보여준다. 데이터 축적 목적 외에, 트레이너 본인에게도 "AI 추정과 내가 실제로 본 값이 이만큼 다르다"는 즉각적인 피드백이 된다.
- **XMSK 데이터 품질 대시보드에 "발가락 마디별 시뮬레이션" 섹션 추가** — 기존 ROM/보행 두 섹션(`server/routes/xmsk.ts`의 `GET /api/xmsk/data-quality`)과 나란히, 20개 발가락 항목마다 "AI 추정 있음"/"트레이너 확인·보정"/"수집률"에 더해 **"평균 보정폭"**(트레이너 값과 AI 추정값의 평균 절대 차이, °) 열을 새로 보여준다. 계산은 새 `server/db.ts`의 `listFootToeSimCalibrationData()`(모든 `foot_sessions`의 `manual_toe_inputs_json`만 읽어옴)로 가져온 원본 JSON을 `xmsk.ts` 핸들러가 직접 집계한다 — ROM/보행과 달리 별도 ground-truth 컬럼이 없고 `FootManualToeInput` 안의 필드만으로 계산 가능해서, 새 DB 컬럼이나 집계 쿼리 없이 기존 "JS에서 직접 집계" 방식을 그대로 따랐다. `avgAbsCorrectionDeg`가 항목별로 꾸준히 크게 나오면 `TOE_SEGMENT_SIM_PARAMS`(결합계수)를 다시 맞출 시점이라는 신호로 읽을 수 있다는 것이 이 열을 넣은 목적이다 — 통계적으로 의미 있으려면 `docs/ai-training-plan.md`가 제시한 기준대로 항목당 최소 수십 건이 필요하다.
- **아직 안 한 것**: 이 데이터로 실제 결합계수를 자동으로 재계산하거나 모델을 학습시키는 파이프라인은 만들지 않았다 — 지금은 "데이터가 충분히 쌓였는지 눈으로 확인할 수 있는 계기판"까지만이고, 실제 재보정/파인튜닝은 데이터가 목표치에 도달한 뒤 별도 작업으로 진행하는 것이 맞다(`docs/ai-training-plan.md`의 로드맵 참고).

**손가락 관절 14개 — "AI 계산 vs 트레이너 실측" 정답값 수집 (2026-09)**: "손가락마디별 시뮬레이션도 나오게 해달라"는 요청에서 시작했지만, 구현 전에 먼저 확인 질문을 던졌다 — 발가락과 달리 손가락 14관절은 이미 MediaPipeHands 21랜드마크로 관절마다 실제 랜드마크를 잡아 직접 계산한 값이라(`HandJointResult`), 발가락처럼 신호 하나를 결합계수로 배분해 "시뮬레이션"할 이유 자체가 없다(오히려 정밀한 실측을 부정확한 배분 추정으로 바꾸는 퇴보가 된다). 세 가지 선택지(① 트레이너 확인/보정 + 보정 데이터 수집, ② 문자 그대로 시뮬레이션 방식으로 전환, ③ 다른 의도)를 제시했고, 사용자가 ①을 선택해 아래와 같이 구현했다.
- **택한 패턴은 발이 아니라 ROM의 `RomXmskGroundTruth`**: 발가락(`FootManualToeInput.source`/`estimatedBeforeValue`/`estimatedAfterValue`)은 "AI 시뮬레이션 초기값을 트레이너가 확인/수정"하는 상태 전이 모델이지만, 손가락은 애초에 시뮬레이션이 아니므로 상태 전이가 필요 없다 — ROM의 `xmskEstimates`처럼 "AI가 이미 계산한 값" 옆에 "트레이너가 실제로 관찰·측정한 값"을 별도 필드로 나란히 쌓아 나중에 계산(포즈 인식 파이프라인) 자체의 정확도를 검증하는 것이 목적이다.
- **새 타입 `HandJointGroundTruth { id, verifiedValue }`** (`src/features/handfoot/types.ts`, `server/types.ts`에 중복 정의 — 기존 프론트/서버 타입 중복 관례) — ROM/보행과 같은 `{id, verifiedValue}` 형태를 그대로 따르되, `id`는 `` `${side}_${joint}` `` 형식이다. ROM의 `JointKey`는 `leftShoulder`처럼 좌우가 이미 키에 포함돼 있지만, 손가락은 `HandJointKey`(`indexMcp` 등)가 `leftResults`/`rightResults` 두 배열에 똑같이 쓰이므로 구분자가 필요했다.
- **before/after 구분 없이 항목당 정답값 하나만 기록** — 이미 ROM의 `xmskEstimates`(값 자체는 `beforeValue`/`afterValue`가 있는데도 `RomXmskGroundTruth`는 `verifiedValue` 하나뿐)와 보행 지표(원래부터 단일 값)가 이런 단순화를 쓰고 있어, 새 패턴을 만들지 않고 기존 관례를 그대로 따랐다.
- **저장**: `hand_sessions`에 `ensureColumn`으로 `hand_ground_truth_json` 추가(기존 `eeg_context_json`과 같은 방식, 기존 행은 NULL) → `HandSessionCreateRequest.handGroundTruth`(선택, 보통 분석 직후엔 비어 있음) → 별도 `PUT /api/hand-sessions/:id/ground-truth` 엔드포인트(`server/routes/handSessions.ts`, ROM의 ground-truth 엔드포인트와 완전히 같은 구조)로 갱신. `HandAnalysisPanel.tsx`(분석 직후 화면)와 `HandSessionHistory.tsx`(저장된 기록 조회 화면) 둘 다 500ms 디바운스로 같은 엔드포인트를 호출한다 — `RomSessionHistory`/`RomApp`이 이미 쓰던 "두 화면 모두 편집 가능, 저장 전이면 로컬 state만 갱신, 저장 후면 PUT" 패턴을 그대로 재사용했다.
- **UI**: `HandResultsPanel.tsx`의 관절별 표(왼손/오른손 각각)에 "트레이너 확인/보정" 열을 추가했다(ROM의 `GroundTruthCell`과 동일한 입력 UI). 사용자가 명시적으로 선택한 문구("트레이너 확인/보정")를 발가락 시뮬레이션 UI와 통일해서 썼지만, 안내 문구는 손가락이 "이미 실제 값을 계산한 것을 검증하는 실측값"이지 "부정확한 값을 대신 채우는 시뮬레이션"이 아니라는 점을 명확히 구분해서 적었다.
- **XMSK 데이터 품질 대시보드에 4번째 섹션 추가** — `server/routes/xmsk.ts`의 `/data-quality` 핸들러에 새 `listHandGroundTruthData()`(`server/db.ts`, `left_results_json`/`right_results_json`/`hand_ground_truth_json`/`calc_version`만 읽음)를 더했다. 항목 라벨은 발가락(`FOOT_ITEM_LABELS`, 프론트 생성 규칙을 정적으로 복제)과 달리 ROM의 `romItemStats`처럼 **동적 discovery** 방식을 썼다 — `HandJointResult`가 이미 `joint`/`label`을 담고 있어 정적 목록을 별도로 유지할 필요가 없기 때문이다(왼손/오른손 구분만 `HAND_SIDE_LABELS`로 라벨 접두사를 붙임). 발가락과 달리 `avgAbsCorrectionDeg`(평균 보정폭) 열은 만들지 않았다 — 손가락엔 "AI가 처음에 낸 시뮬레이션 값"이라는 게 애초에 없으므로(원본 계산값이 바로 실측 대상이지, 트레이너가 고쳐 쓸 대상이 아님) 이 지표 자체가 성립하지 않는다. `XmskDataQualityDashboard.tsx`에 `groundTruthLabel="트레이너 확인/보정"`만 지정하고 `correctionLabel`은 생략한 네 번째 `<FeatureSection>`을 추가했다.

**XMSK** (`src/features/xmsk/XmskApp.tsx`):
- Not built on the shared pose pipeline — no video/pose involved at all. Gated by a password prompt (`XmskPasswordGate`); the resulting bearer token is cached in `localStorage` (`xmsk_token`) and attached to every API call, and any `401` response (via `XmskAuthError`, thrown from `xmskApi.ts`) re-locks the app.
- Once unlocked, a second-level tab switch (`XmskModule`: `recipes` | `dictionary` | `evaluation`) picks the module:
  - **통증 레시피 (recipes)** — `XmskRegionPicker` picks one of 8 `XmskRegionKey`s (neck/shoulder, low back, knee, ankle, hip, elbow, wrist, upper back), then `XmskRecipeFlow` walks a fixed stage sequence (`XmskFlowStage`: `redflag → before → recipe → after → summary`) driven by static per-region content in `xmskRecipes.ts` (`XMSK_RECIPE_MAP`) — Red Flag checklist, before/after manual measurement inputs, ordered steps, closing script. A header (visible across every stage, mirroring the evaluation form's name/evaluator fields) collects `clientName` (required) and `trainerName` (optional). On save it posts these plus before/after measurements to `/api/xmsk/sessions`; `XmskSessionHistory` lists past sessions (client name shown per row) and reuses `ComparisonBarChart`.
  - **근육 사전 (dictionary)** — `XmskMuscleDictionary` is a pure static reference browser over `xmskMuscles.ts` (origin/insertion, action, stretch cue with common error responses, and manual release technique per muscle, grouped by region). No backend calls.
  - **평가표 (evaluation)** — `XmskEvaluationForm` scores a trainee against fixed sections/items defined in `xmskEvaluation.ts` (`XMSK_EVAL_SECTIONS`: theory/practical/safety/CS, each with scored items and pass/fail required items) and computes a verdict client-side (`computeXmskEvalVerdict`: `hold` if any required item fails, else `approved` ≥70, `retry` 60–69, else `hold`) before posting to `/api/xmsk/evaluations`; `XmskEvaluationHistory` lists past evaluations. The scoring weights/thresholds are duplicated on the server (`server/xmskEvalDefs.ts`) since the server recomputes the verdict rather than trusting the client's.
  - **데이터 품질 (dataQuality, 2026-09)** — `docs/ai-training-plan.md` 로드맵의 "데이터 품질 대시보드" 항목. 트레이너를 위한 도구가 아니라 이 앱을 만드는 사람이 "AI 추정 계산 로직을 보정할 만큼 트레이너 실측값이 쌓였는지" 확인하는 내부 참고 화면이라, 별도 인증 체계를 새로 만드는 대신 이미 있는 XMSK 비밀번호 게이트 뒤에 그냥 네 번째 모듈로 얹었다. `XmskDataQualityDashboard`는 `GET /api/xmsk/data-quality`(다른 XMSK 라우트와 같이 `requireAuth` 필요) 결과를 ROM/보행 두 섹션으로 나눠, AI 추정치가 계산된 적 있는 항목마다 "AI 추정 있음 건수" / "트레이너 실측값 있음 건수" / "수집률(%)"을 표로 보여주고, 전체 기록 수와 `calcVersion`별 분포도 함께 보여준다. 서버 집계 로직(`server/routes/xmsk.ts`의 `GET /data-quality` 핸들러)은 `db.ts`의 `listRomGroundTruthData()`/`listGaitGroundTruthData()`(전체 세션/진단 기록에서 추정치·실측값·버전 컬럼만 뽑아오는 별도 조회 — 개수 제한 없음, 개인용 앱 규모라 전체를 한 번에 읽어도 무리 없다는 전제)를 돌며 JS에서 직접 집계한다 — 새 SQL 집계 쿼리나 별도 통계 테이블 없이 기존 `*_json` 컬럼을 그대로 파싱하는, 이 프로젝트의 "의존성 최소화" 기조를 따른 방식. ROM은 XMSK 추정치 JSON 자체에 `label`/`unit`이 이미 들어있어 그대로 쓰지만, 보행 ground truth JSON은 `{id, verifiedValue}`만 저장하므로 항목의 라벨/단위는 프론트엔드 `GAIT_VERIFIABLE_ITEMS`(`src/features/gait/lib/gaitGroundTruth.ts`)를 표시용으로 서버에 그대로 복제해뒀다(기존 프론트/서버 타입 중복 관례). 보행의 "AI 추정 있음" 판정은 개별 지표마다 `gait_metrics_json`을 파싱하는 대신, `GaitGroundTruthPanel`이 애초에 걸음이 2보 이상 감지됐을 때만(그리고 4개 항목을 한 번에) 렌더링된다는 사실을 그대로 활용해 이미 컬럼으로 갖고 있는 `total_steps >= 2`를 기준으로 삼는다.
- `src/features/xmsk/types.ts` — all XMSK request/response and domain types (`XmskRecipe`, `XmskMuscle`, `XmskEvalSection`, session/evaluation create+list+detail shapes, etc).
- `src/features/xmsk/lib/xmskApi.ts` — fetch wrappers for `/api/xmsk/*`, attaching the bearer token and normalizing `401` into `XmskAuthError`.

**뇌파(EEG) 실시간 표시** (`src/features/eeg/EegApp.tsx`, 2026-09):
- **요청 배경**: 사용자가 "패스트볼(Fastball/FPVS) EEG 치매 검사" 기사와 SCAN(Somato-Cognitive Action Network) 이론 자료를 공유한 뒤(자세한 검토 결과는 위 보행 분석 절의 "검토했지만 적용하지 않은 것" 참고), 곧이어 "소비자용 EEG 헤드밴드 착용 시 측정 결과를 앱에 실시간으로 나타낼 수 있도록" 기능을 명시적으로 요청했다. 두 차례의 확인 질문 끝에(전용 네이티브 앱 설치가 필요한 Emotiv 계열은 제외하고) 브라우저에서 바로 연결되는 **Muse(2/S 등)** 한 종류만 지원하기로 확정했다.
- **왜 muse-js(v3.3.0)를 골랐나 — 그리고 왜 web-muse는 아닌가**: 처음 후보였던 `web-muse`(React 훅 API가 더 편하고 목업 모드도 있음)는 `https://registry.npmjs.org/web-muse/latest`가 404를 반환해 npm 공개 레지스트리에 실제로 배포돼 있지 않다는 게 확인됐다(문서에도 `npm link`/로컬 경로/깃허브 URL 설치만 안내돼 있음) — 이 프로젝트의 배포 파이프라인(`Dockerfile`의 `npm install`)이 설치할 수 없는 패키지라 제외했다. 대신 오래됐지만 실제로 공개 배포된 `muse-js@3.3.0`을 선택했다. **이전 실수에서 배운 검증 방식**: `@tensorflow-models/hand-pose-detection`을 기억/추정만으로 추가했다가 실제 배포에서 두 번 실패한 적이 있어(위 mediapipe-hands-shim 항목 참고), 이번엔 그 반성으로 실제로 배포된 파일 내용을 읽고 검증한 뒤 채택했다 — 이 개발 샌드박스는 npmjs.com 사람용 페이지는 막혀 있지만(403), `registry.npmjs.org/<pkg>/latest`(메타데이터 JSON)와 `unpkg.com/<pkg>@<version>/<path>`(실제 배포된 소스 파일 원문)는 둘 다 가져와지므로, `dist/muse.d.ts`/`dist/muse.js`/`dist/lib/muse-interfaces.d.ts`/`dist/lib/muse-parse.js`를 직접 읽어 정확한 export 형태, 의존성(`rxjs`, `@types/web-bluetooth`), 그리고 내부 단위 변환 공식까지 확인했다. 이 방법은 새 3rd-party 의존성을 추가할 때 앞으로도 쓸 만한 절차로 남겨둔다.
- **muse-js가 이미 처리해주는 것 — 단위 변환**: `dist/lib/muse-parse.js` 원문에서 확인한 실제 변환 공식은 `µV = 0.48828125 × (원시 12비트 ADC값 − 2048)`이며, 이 변환은 라이브러리 내부에서 이미 끝난 채로 `EEGReading.samples`가 나온다 — 이 앱의 코드에서 다시 변환할 필요가 없다.
- **Web Bluetooth 제약**: `navigator.bluetooth`는 데스크톱/안드로이드의 Chrome·Edge·삼성인터넷 등에서만 있고, **iOS Safari에는 애플이 아예 이 API를 넣지 않았고, 카카오톡 등 인앱 웹뷰에서도 못 쓴다**. `museConnection.ts`의 `isWebBluetoothSupported()`로 감지해 `EegApp`이 미지원 브라우저엔 안내 문구만 보여준다. 또한 `MuseClient.connect()` 내부의 `navigator.bluetooth.requestDevice()`(기기 선택 팝업)는 **사용자 제스처(클릭)로부터 이어지는 동기 호출 스택 안에서만** 뜬다 — `await`를 하나라도 먼저 거치면 브라우저가 팝업을 막는다. 이건 이 프로젝트가 PDF 인쇄 기능(`window.print()`)에서 이미 겪은 것과 같은 종류의 제약이라, `EegApp.handleConnect`도 클릭 즉시 `connectMuse()`를 호출하도록 짰다.
- **왜 서버에 전혀 저장하지 않는가(이 앱의 다른 5개 기능과의 결정적 차이)**: (1) 사용자의 요청 자체가 "실시간으로 나타낼 수 있도록"이지 "기록 저장"이 아니었고, (2) 뇌파는 이 앱이 다루는 어떤 데이터보다 민감한 생체 신호라 요청받지 않은 서버 저장을 미리 추가하지 않는 게 안전하다고 판단했다. 그래서 이 기능만 `server/`에 대응하는 라우트/테이블이 전혀 없고, 화면을 벗어나거나 새로고침하면 그 순간까지의 데이터는 전부 사라진다.
- **파형/대역파워 렌더링 — producer/consumer 분리**: Muse는 채널당 초당 약 256개 샘플을 12개씩 묶어 보내는데, 샘플마다 React state를 갱신하면 초당 수백 번의 리렌더가 발생해 감당이 안 된다. `src/features/eeg/lib/eegBuffer.ts`의 `EegBuffers`(채널별 `Float64Array` 링 버퍼)가 `poseDetector.ts`와 같은 producer/consumer 철학을 따른다 — BLE 콜백(producer, `EegApp`이 `useRef`로 들고 있음)은 버퍼에 값을 채워 넣기만 하고 React state를 건드리지 않으며, `EegWaveformChart`(consumer)는 그와 독립적인 `requestAnimationFrame` 루프가 매 프레임 최신 스냅샷을 읽어 캔버스에 그린다(스타일은 `SkeletonViewer.tsx`의 canvas+useRef 관례를 참고했지만, 그쪽은 프레임 인덱스가 바뀔 때만 다시 그리는 반면 여기는 계속 흐르는 데이터라 rAF 루프로 바꿨다). 파형 창은 최근 4초(`WAVEFORM_WINDOW_SECONDS`), 채널마다 그 프레임에서의 최대 진폭 기준으로 자동 스케일한다.
- **대역 파워 계산 — 의존성 없는 자체 FFT**: `src/features/eeg/lib/fft.ts`에 라디오-2 Cooley-Tukey FFT를 직접 구현했다(새 npm 패키지 추가 없이). 이유: FFT는 검증 가능한 표준 알고리즘이라 직접 짜서 정확성을 스스로 추론할 수 있는 반면, 낯선 서드파티 패키지는 실제 배포 파일을 읽어보지 않는 한 정확한 동작을 확신할 수 없다(이번에 muse-js를 고를 때 적용한 것과 같은 원칙). Muse의 `EEG_FREQUENCY`가 정확히 256Hz라, 256샘플(=1초) 윈도우에 Hann 윈도우를 씌워 FFT를 돌리면 정확히 1Hz/bin 해상도가 나와 대역(델타 0.5–4·세타 4–8·알파 8–13·베타 13–30·감마 30–45Hz) 매핑이 "그 범위 bin들의 파워 합"으로 단순해진다. `EegBandPowerBars`는 이 계산을 rAF가 아니라 0.5초 간격 `setInterval`로만 돌리고(파형처럼 60fps로 볼 필요가 없고 FFT는 계산량도 더 있음), 화면에 보이는 4채널(TP9/AF7/AF8/TP10, AUX 제외)의 평균을 막대로 보여준다 — 절대적인 의학적 기준값이 아니라 화면 안에서의 상대적 변화 참고용이라는 점을 UI 문구로도 명시한다.
- `src/features/eeg/types.ts` — 채널 이름/표준 대역 정의 등 순수 타입·상수(서버 API 타입 없음 — 위 설명대로 백엔드가 없다).
- `src/features/eeg/lib/museConnection.ts` — `MuseClient`를 감싸는 얇은 래퍼. `connectMuse(callbacks)`는 반드시 클릭 핸들러의 동기적 첫 동작으로 호출돼야 한다는 제약을 주석으로 강조해뒀다.
- 새 npm 의존성: `package.json`에 `muse-js`(및 그 의존성 `rxjs`, `@types/web-bluetooth`)가 추가됐다 — `Dockerfile`이 이미 `npm ci`가 아니라 `npm install`을 쓰도록 바뀌어 있어(위 mediapipe-hands-shim 항목 참고) 배포 VM이 실제 인터넷으로 새로 설치·해석한다.
- **아직 안 한 것**: 배터리 표시는 `TelemetryData`에서 받아오지만 자이로/가속도계(`gyroscopeData`/`accelerometerData`)나 PPG(`enablePpg`)는 아예 켜지 않는다(요청 범위 밖). 여러 명이 번갈아 착용하는 시나리오나, 세션을 저장해 나중에 다시 보는 기능은 의도적으로 만들지 않았다(위 "왜 서버에 저장 안 하는가" 참고) — 나중에 필요해지면 별도 요청으로 다뤄야 한다.
- **실제 배포 후 발견된 버그 — "Cannot read properties of undefined (reading 'subscribe')"(2026-09)**: 처음 버전의 `museConnection.ts`는 `new MuseClient()` 직후, `client.connect()`를 부르기도 전에 `client.eegReadings.subscribe(...)`/`client.telemetryData.subscribe(...)`를 걸어서 실기기 테스트에서 이 에러가 났다. unpkg.com의 muse-js 실제 배포 소스(`dist/muse.js`)를 다시 읽어 확인해보니, 생성자에서 바로 만들어지는 건 `connectionStatus`(`new BehaviorSubject(false)`) 하나뿐이고 `eegReadings`/`telemetryData`/`gyroscopeData`/`accelerometerData`/`ppgReadings`/`rawControlData`/`controlResponses`/`eventMarkers`는 전부 `connect()` 안에서야 실제 rxjs 객체로 채워진다 — 그 전엔 `undefined`라 `.subscribe()`를 걸면 정확히 이 에러가 난다. **고쳤다**: `eegReadings`/`telemetryData`/`connectionStatus` 구독을 전부 `await client.connect()` 완료 뒤로 옮겼다(`connect()` 실패 시엔 애초에 구독할 대상이 없으므로 옵셔널 체이닝으로 정리).
- **에러 문구는 한글이 먼저 나오게(2026-09, 사용자 요청)**: Web Bluetooth/muse-js가 던지는 에러는 전부 영어 원문이라(예: "User cancelled the requestDevice() chooser.") 그대로 화면에 띄우면 무슨 뜻인지 알 수 없다. `src/features/eeg/lib/errorMessages.ts`의 `translateMuseError()`가 자주 나오는 패턴(사용자 취소, 블루투스 꺼짐, GATT 연결 끊김, 사용자 제스처 타이밍 문제)을 한글 안내문으로 바꾸고, 못 알아본 나머지는 일반적인 한글 안내 + 원문을 작은 글씨로 참고용 표시한다. `EegApp`의 에러 표시는 항상 `{korean, raw}` 형태를 쓰고, 한글 문구가 위에 크게, 원문은 아래에 작게 나온다 — 다른 기능에 새로 에러 문구를 추가할 때도 이 원칙(한글이 먼저/크게, 원문은 참고용 보조)을 따를 것.

**XCTS — 심혈관/전신 컨디션 측정 도구 모음** (`src/features/xcts/XctsApp.tsx`, 2026-09):

- **요청 배경**: Muse 뇌파(EEG)에 이어 "Muse 헤드밴드처럼 뇌파 측정기기뿐만 아니라 스마트워치같은 외부연결기기를 검색해서 mydoctor앱에 다양한 생체데이터분석 기능을 추가하는 것을 검토해달라"는 조사 요청(`docs/wearable-biometric-integration-review.md`)에 이어, "진행하는데 이번에는 결과를 XMSK 내부에 두지 말고 XCTS를 새로 만들어서 해달라"는 명시적 지시로 착수했다. 세 가지를 확인 질문으로 미리 정했다: ① XCTS의 성격은 "심박 측정" 단일 기능이 아니라 "심혈관/전신 컨디션 측정 도구 모음"이라는 넓은 확장 전제(추후 다른 생체 측정 도구가 이 탭 아래 더 생길 수 있음), ② 인증은 XMSK와 같은 방식(별도 비밀번호를 새로 만들지 않고 앱 전체 로그인을 재사용), ③ 데이터 모델은 손·발/ROM 세션에 딸린 부가 정보가 아니라 XCTS 자체의 독립 세션.
- **왜 XMSK 안에 넣지 않았나**: XMSK는 "AI가 계산한 추정치 vs 트레이너 실측값"을 비교·보정하는 성격의 도구(통증 레시피, 근육 사전, 평가표, 데이터 품질 대시보드)인 반면, XCTS는 센서가 직접 잰 원본 측정값(심박수·RR간격에서 계산한 HRV)을 그대로 기록하는 성격이 근본적으로 다르다 — 그래서 XMSK의 데이터 품질 대시보드(`GET /api/xmsk/data-quality`)에도 XCTS를 포함하지 않았다(비교할 "AI 추정 vs 실측"이라는 쌍 자체가 없음).
- **왜 표준 BLE 심박 센서인가 — 그리고 왜 일반 스마트워치는 안 되는가**: 조사 결과(`docs/wearable-biometric-integration-review.md`) 블루투스 SIG가 표준화한 **심박 서비스(Heart Rate Service, 0x180D)와 심박 측정 특성(Heart Rate Measurement, 0x2A37)**은 `navigator.bluetooth`(Web Bluetooth)로 벤더 SDK나 OAuth 없이 바로 읽을 수 있는 공개 프로토콜이고, Polar H10/Verity Sense, Wahoo Tickr 같은 가슴띠형 센서가 이를 그대로 지원한다. 반면 Apple Watch·갤럭시 워치·Fitbit·Whoop·Oura 같은 **일반 소비자용 스마트워치는 실시간 센서 값을 자체 앱/클라우드 안에 가둬두고 이 표준 서비스로 내보내지 않는다** — 그래서 이 기능은 가슴띠형 센서 전용이며, `errorMessages.ts`의 "기기를 찾을 수 없음" 에러 문구에도 이 한계를 직접 안내한다. muse-js를 고를 때와 마찬가지로 실제로 검증 가능한 표준 스펙을 기준으로 구현했다 — 벤더 SDK를 추측으로 흉내내지 않았다.
- **확장 가능한 데이터 소스 설계 — 사용자의 명시적 요청("추후 스마트워치 제조사 또는 개발사와 협업할 경우도 고려해서 진행해달라")**: `XctsMeasurementSource`(현재 `'ble-heart-rate'` 하나)와 `XctsModule`(현재 `'heartRate'` 하나)를 처음부터 확장 가능한 유니온 타입으로 설계했다 — 나중에 특정 스마트워치 제조사/개발사와 실제로 SDK 연동을 하게 되면 이 유니온에 새 값을 추가하는 것만으로 세션 데이터 모델이나 결과 화면(`XctsResultsPanel`)을 다시 설계할 필요가 없게 하는 것이 목적이다. 연결 계층(`bleHeartRate.ts`의 콜백 형태 — connect → `HeartRateSample` 스트림 → `HeartRateWindowSummary`로 요약)도 이 교체 가능성을 염두에 두고 분리해뒀다. 서버 쪽 검증(`server/routes/xctsSessions.ts`)도 `deviceSource`가 `'ble-heart-rate'` 문자열과 정확히 같은지 확인하지 않고 "비어있지 않은 문자열인지"만 검사한다 — 나중에 새 소스 값이 추가돼도 서버 코드를 같이 고칠 필요가 없도록 한 것.
- **HRV(RMSSD) 계산 — 60초 캡처 창, EEG의 1초 순간 스냅샷과 다른 이유**: 뇌파 대역파워는 256샘플(1초) FFT 창이면 충분하지만, 심박변이도(HRV)는 연속된 RR간격들의 짧은 시계열이 필요해 순간값으로는 계산할 수 없다. `HeartRateCaptureControl.tsx`는 `CAPTURE_WINDOW_MS = 60_000`(1분) 동안 샘플을 모은 뒤 `computeRmssd()`(연속 RR간격 차이의 제곱평균제곱근)로 HRV를 낸다 — 표준 HRV 프로토콜(보통 1~5분 이상)보다 짧고, PPG(광혈류측정) 기반 손목 기기보다 신뢰도가 높은 가슴띠 ECG 기준이라는 두 가지 이유로 HRV 값은 `evidence: 'experimental'`로, 평균 심박수 자체는 `evidence: 'approximate'`로 구분해 태깅한다(`heartRateInterpretation.ts`) — ROM/보행/손발이 이미 쓰는 "근거 수준 태그 + 방법론 `<details>`" 관례를 그대로 따랐다.
- **인증 — 새 비밀번호를 만들지 않고 기존 앱 전체 로그인 재사용**: 처음엔 "XMSK처럼 비밀번호 게이트"가 "XMSK와 같은 방식으로 별도 비밀번호를 새로 만든다"는 뜻으로도 읽힐 수 있었지만, 실제 코드(`App.tsx`의 `GATED_TABS`/`token` state, `server/xmskAuth.ts`의 `requireAuth`)를 확인해보니 **이 앱은 애초에 트레이너 도구마다 다른 비밀번호를 두지 않고 보행/ROM/손발/XMSK 4개 탭이 이미 하나의 앱 전체 로그인을 공유하고 있었다**(위 "앱 전체 인증" 절 참고, 이유: "트레이너용 도구 여러개에 서로 다른 비밀번호를 두는 건 불편하고 코드도 늘어난다"). 그래서 XCTS도 새 비밀번호를 만들지 않고 이 기존 메커니즘에 다섯 번째 탭으로 합류시켰다 — `App.tsx`의 `GATED_TABS`에 `'xcts'` 추가, `server/routes/xctsSessions.ts`도 나머지 4개 라우터와 똑같이 `xctsSessionsRouter.use(requireAuth)`.
- **Web Bluetooth 타입 — 새 npm 의존성을 추가하지 않았다**: `navigator.bluetooth`/`BluetoothRemoteGATTCharacteristic` 등에 타입을 붙이려면 보통 `@types/web-bluetooth`가 필요하지만, `package.json`을 확인해보니 이 프로젝트는 이 패키지를 쓰지 않고 있었다(다른 곳은 전부 `'bluetooth' in navigator`처럼 타입 없이 확인). 새 의존성을 추가하는 대신(이 프로젝트의 "의존성 최소화" 기조), `src/shared/lib/heartRate/bleHeartRate.ts`에 실제로 쓰는 부분(`requestDevice`/`gatt.connect`/`getPrimaryService`/`getCharacteristic`/`startNotifications`/`characteristicvaluechanged`)만 최소한으로 로컬 인터페이스로 선언해 썼다.
- **저장 — XCTS 전용 독립 세션 테이블**: `xcts_sessions` 테이블(`server/db.ts`)은 hand_sessions/foot_sessions처럼 "영상 before/after"에 딸린 것이 아니라 그 자체로 하나의 완결된 세션이다(사용자가 명시적으로 선택한 데이터 모델). `POST /api/xcts-sessions`(`clientName` 필수, `baseline`/`post` 중 최소 하나 필수), `GET /api/xcts-sessions?limit=`(목록에 `avgHeartRateDeltaBpm` 서버 계산값 포함), `GET /api/xcts-sessions/:id`, `DELETE /api/xcts-sessions/:id` — ROM/보행/손/발의 ground-truth·manual-input류 `PUT` 엔드포인트는 없다(비교 대상 AI 추정치가 없으므로 트레이너가 나중에 값을 "확인/보정"할 대상 자체가 없음).
- **아직 안 한 것**: 지금은 `XctsModule`이 `'heartRate'` 하나뿐이라 `XctsApp.tsx`에 XMSK 같은 모듈 탭 전환 UI가 없다 — 두 번째 도구(예: 혈압, 산소포화도)가 추가되면 그때 탭 UI를 얹으면 된다. 스마트워치 제조사/개발사와의 실제 SDK 연동도 아직 없다 — 위 확장 가능한 타입 설계는 그 시점이 왔을 때 세션 모델을 다시 설계하지 않아도 되게 하기 위한 준비일 뿐, 지금 당장 그런 연동을 구현한 것은 아니다.

### Backend (`server/`)

Minimal Express server, separate from the frontend's `tsconfig.app.json`/build (its own `server/tsconfig.json`, checked via `npm run typecheck:server`, not part of `npm run build`).

- `server/index.ts` — Express app; mounts `/api/auth`, `/api/rom-sessions`, `/api/hand-sessions`, `/api/foot-sessions`, `/api/gait-diagnostics`, `/api/xmsk` and `/api/xcts-sessions`; also serves `dist/` static + SPA fallback (so `npm run build && npm run start` runs frontend + API from one process). Sets `app.set('trust proxy', 1)` — needed so `req.ip` is the real client IP (from Caddy's `X-Forwarded-For`) rather than Caddy's own address, which the login rate limiter (below) keys on.
- `server/db.ts` — `node:sqlite`'s `DatabaseSync` opens `server/data/rom.db` (gitignored; directory kept via `server/data/.gitkeep`), creates the `stretch_sessions`, `xmsk_sessions`, `xmsk_evaluations`, `gait_diagnostics`, `hand_sessions`, `foot_sessions`, and `xcts_sessions` tables on startup. No native dependency — relies on Node's built-in SQLite (requires a recent Node; confirmed working on v24).
- `server/routes/romSessions.ts` — `POST /api/rom-sessions` (requires `clientName` and exactly 8 `results`, `trainerName`/`xmskEstimates`/`xmskManualInputs`/`xmskGroundTruth`/`calcVersion` optional), `GET /api/rom-sessions?limit=`, `GET /api/rom-sessions/:id` (includes `xmskEstimates`/`xmskManualInputs`/`xmskGroundTruth`/`calcVersion`), `PUT /api/rom-sessions/:id/manual-inputs` (replaces just the `xmskManualInputs` array on an already-saved session — see `romApi.ts` above for why this is separate from the create call), `PUT /api/rom-sessions/:id/ground-truth` (same idea, for `xmskGroundTruth` — see the ground-truth section above), `DELETE /api/rom-sessions/:id`, with hand-rolled request validation. `xmskEstimates`/`xmskManualInputs`/`xmskGroundTruth` (see `src/features/rom/lib/xmskEstimates.ts` and the ground-truth note above) are stored in their own `xmsk_estimates_json`/`xmsk_manual_inputs_json`/`xmsk_ground_truth_json` columns, kept separate from the original `results_json` specifically so the existing "exactly 8 results" validation never has to change. `calcVersion` (see the 결과-버전 태깅 section above) is stored as-is in its own `calc_version` column — the server only checks it's a string, never interprets it. All routes require auth (`romSessionsRouter.use(requireAuth)`) since 2026-09 — see "앱 전체 인증(보안 점검)" below.
- `server/routes/handSessions.ts` — `POST /api/hand-sessions` (requires `clientName`, `leftResults`/`rightResults` arrays of 14 items each — empty-valued entries when that hand wasn't detected, not an error), `GET /api/hand-sessions?limit=`, `GET /api/hand-sessions/:id`, `DELETE /api/hand-sessions/:id`. No manual-input/ground-truth endpoints yet (see 손·발 분석 section above). All routes require auth since 2026-09 (`handSessionsRouter.use(requireAuth)`).
- `server/routes/footSessions.ts` — `POST /api/foot-sessions` (requires `clientName`; `toeEstimates`/`manualToeInputs`/`calcVersion` optional), `GET /api/foot-sessions?limit=`, `GET /api/foot-sessions/:id`, `PUT /api/foot-sessions/:id/manual-toe-inputs` (replaces the `manualToeInputs` array — mirrors ROM's manual-inputs endpoint; all 발가락 10마디 data is trainer-entered, so this is the primary write path after the initial save), `DELETE /api/foot-sessions/:id`. All routes require auth since 2026-09 (`footSessionsRouter.use(requireAuth)`).
- `server/routes/gaitDiagnostics.ts` — `POST /api/gait-diagnostics` (`clientName`/`trainerName`/`groundTruth`/`calcVersion` optional — the record auto-saves on every analysis run whether or not the trainer typed a name first — plus `sampledTimesChecksum`, a debug field, see `poseDetector.ts`), `GET /api/gait-diagnostics?limit=&search=` (LIKE match on video name/client/trainer/device info/note), `PUT /api/gait-diagnostics/:id/ground-truth` (replaces just the `groundTruth` array — mirrors ROM's ground-truth endpoint; needed a new `getGaitDiagnostic(id)`/`get`-by-id in `db.ts` since this table previously only supported list/insert/delete), `DELETE /api/gait-diagnostics/:id`. All routes require auth since 2026-09 (`gaitDiagnosticsRouter.use(requireAuth)`) — previously had none (see 보안 점검 section below for why that mattered).
- `server/routes/xctsSessions.ts` (2026-09) — `POST /api/xcts-sessions` (requires `clientName` and a non-empty `deviceSource`; `baseline`/`post` each optional but at least one required — shallow-validated like `eegContext`, since the server never interprets `HeartRateWindowSummary`'s contents), `GET /api/xcts-sessions?limit=` (list, with `avgHeartRateDeltaBpm` computed server-side as `post.avgHeartRateBpm - baseline.avgHeartRateBpm` when both are present), `GET /api/xcts-sessions/:id`, `DELETE /api/xcts-sessions/:id`. No manual-input/ground-truth `PUT` endpoints (see XCTS section above — there's no AI estimate to compare against). All routes require auth (`xctsSessionsRouter.use(requireAuth)`), reusing the same shared token as the other 4 gated tabs.
- `server/routes/auth.ts` — `POST /api/auth/login` (2026-09): the new shared login endpoint for all 4 gated tabs. Same `checkPassword`/`issueToken` as XMSK's old `/api/xmsk/auth`, so tokens are interchangeable; rate-limited via `xmskAuth.ts`'s `isLoginRateLimited`/`recordFailedLogin`/`clearLoginAttempts`.
- `server/routes/xmsk.ts` — `POST /api/xmsk/auth` (password → bearer token; kept for backward compatibility, now also rate-limited — new code should use `POST /api/auth/login` instead); `POST/GET /api/xmsk/sessions`, `GET/DELETE /api/xmsk/sessions/:id`; `POST/GET /api/xmsk/evaluations`, `GET/DELETE /api/xmsk/evaluations/:id`; `GET /api/xmsk/data-quality` (data-quality dashboard summary — see the "데이터 품질" module note above). Every route but `/auth` runs through the `requireAuth` middleware (imported from `xmskAuth.ts` — used to be a local function here, moved out in 2026-09 so the other 4 routers could share it) that verifies the `Authorization: Bearer` token. Evaluation verdicts are recomputed server-side from `xmskEvalDefs.ts`, never trusted from the request body.
- `server/xmskAuth.ts` — single shared password (`XMSK_PASSWORD` env var, dev fallback `forestretch1`) checked with `timingSafeEqual`; issues a signed, self-contained token (`base64url(JSON{exp}).HMAC-SHA256`, 12h TTL, secret from `XMSK_SECRET` env var) — no session store, no per-user identity. Locally, `dev:server`/`start` load these from a root `.env` via tsx's `--env-file-if-exists` (see `.env.example`); **whatever platform this app is deployed to, `XMSK_PASSWORD` and `XMSK_SECRET` must be set there too** — the dev fallbacks are not safe for production. Also exports (2026-09): `requireAuth` (shared auth middleware, see above) and a hand-rolled login rate limiter (`isLoginRateLimited`/`recordFailedLogin`/`clearLoginAttempts` — in-memory `Map` keyed by `req.ip`, 8 failed attempts per 10 minutes; no new npm dependency, per this project's minimal-deps policy — see "앱 전체 인증" below for why this was added).
- `server/xmskEvalDefs.ts` — server-side mirror of the evaluation scoring weights/thresholds and `computeXmskEvalVerdict`, kept in sync by hand with `src/features/xmsk/lib/xmskEvaluation.ts`.
- `server/types.ts` — request/response shapes, intentionally duplicated (not imported) from `src/features/rom/types.ts` and `src/features/xmsk/types.ts` since the two `tsconfig`s use different `moduleResolution`.
- In dev, Vite proxies `/api/*` to `http://localhost:3001` (`vite.config.ts` `server.proxy`) so the two processes need no CORS setup.

TypeScript project references: root `tsconfig.json` → `tsconfig.app.json` (`src/`) + `tsconfig.node.json` (`vite.config.ts` only). `server/tsconfig.json` is standalone, not referenced from the root — `npm run build` never touches `server/`.

Linting is via Oxlint (`.oxlintrc.json`), not ESLint — rules currently enabled: `react/rules-of-hooks`, `react/only-export-components`. Oxlint also covers `server/`.

All features (보행/ROM/손·발/XMSK/뇌파) are reference/aid tools only, not medical diagnostics — stated in each feature's UI, and should stay true of any feature added.

### 앱 전체 인증(2026-09 보안 점검)

사용자가 "지금까지 작업에 대한 보안은 철저하게 되는거야?"라고 물어본 게 계기가 돼 실제 코드(`.env.production`, 각 라우터, `Dockerfile`/`docker-compose.yml`/`Caddyfile`)를 직접 훑어본 보안 점검. 대부분(HTTPS는 Caddy가 자동 처리, SQL 인젝션은 `db.ts`가 전부 `?` 플레이스홀더 사용, 시크릿은 `.dockerignore`로 이미지에 안 들어감)은 문제없었지만, **치명적인 구멍 하나**를 발견해 그 자리에서 고쳤다:

- **발견한 문제**: XMSK만 비밀번호로 잠겨 있었고, 보행(`gait-diagnostics`)/ROM(`rom-sessions`)/손발(`hand-sessions`, `foot-sessions`) API는 인증이 전혀 없었다. 즉 로그인 없이 누구나 회원 이름이 담긴 세션을 만들고, 목록을 읽고, 지울 수 있었다. 특히 `HomeScreen.tsx`(로그인 전에도 보이는 홈 화면)가 최근 ROM 기록 3건을 미리보기로 가져오는데, 그 응답 JSON에 `clientName`이 그대로 포함돼 있어서 — 화면엔 `videoName`만 보이지만 — 로그인하지 않은 누구나 브라우저 개발자 도구 네트워크 탭으로 회원 이름을 볼 수 있는 실제 노출 경로였다.
- **로그인 시도 속도 제한도 없었다**: `POST /api/xmsk/auth`에 무차별 대입(비밀번호를 계속 찔러보는) 방어가 전혀 없었다.

**고친 방식** — XMSK가 이미 쓰던 비밀번호/토큰 방식(`server/xmskAuth.ts`의 `checkPassword`/`issueToken`, HMAC 서명 토큰, 12시간 TTL)을 그대로 재사용해 4개 탭 전체로 확대했다. 새 인증 시스템을 새로 설계하지 않은 이유: 이미 검증된 방식이 있는데 트레이너용 도구 4개에 서로 다른 비밀번호/로그인 화면을 두는 건 트레이너 입장에서도 불편하고 코드도 불필요하게 늘어난다.

- `server/xmskAuth.ts`의 `requireAuth` 미들웨어를 `routes/xmsk.ts`의 로컬 함수에서 꺼내 export하고, 나머지 4개 라우터(`romSessions.ts`/`gaitDiagnostics.ts`/`handSessions.ts`/`footSessions.ts`)가 각각 `<router>.use(requireAuth)`로 라우터 전체를 잠갔다.
- 로그인 시도 속도 제한: `express-rate-limit` 같은 새 npm 패키지를 추가하는 대신(이 프로젝트의 "테스트 러너도 없이 의존성을 최소로 유지" 방침), `xmskAuth.ts` 안에 메모리 `Map`으로 IP별 실패 횟수만 세는 방식으로 직접 구현(`isLoginRateLimited`/`recordFailedLogin`/`clearLoginAttempts`, 10분에 8회). `server/index.ts`에 `app.set('trust proxy', 1)`을 추가해야 했다 — Caddy 리버스 프록시 뒤에서는 이게 없으면 `req.ip`가 Caddy의 주소로 잡혀 IP별 제한이 사실상 전체 사용자를 한 IP로 묶어버린다.
- 새 공용 로그인 엔드포인트 `POST /api/auth/login`(`server/routes/auth.ts`)을 추가하고 `server/index.ts`에 마운트. 기존 `POST /api/xmsk/auth`는 하위 호환을 위해 남겨두고 똑같이 속도 제한을 적용했다 — 두 엔드포인트 모두 같은 `checkPassword`/`issueToken`을 쓰므로 토큰은 완전히 동일하다.
- 프론트엔드: XMSK 전용이던 `src/features/xmsk/lib/xmskApi.ts`의 인증 부분(`XmskAuthError`/`authHeaders`/`xmskAuth`)을 일반화해 `src/shared/lib/authApi.ts`로 새로 뺐다(`AuthError`, `login`, `authHeaders`, `getStoredToken`/`storeToken`/`clearStoredToken`, `parseAuthAwareJson`/`expectNoContentAuthAware`) — 이 프로젝트는 기능끼리 서로의 `lib`/`components`를 import하지 않는 관례(예외는 `HomeScreen→rom` 하나뿐)라 `xmskApi.ts`를 다른 기능이 그대로 가져다 쓰는 대신 `shared` 아래에 새로 두는 게 맞다고 판단했다. 토큰 저장 키는 `'xmsk_token'` 그대로 유지(하위 호환 — 기존에 로그인해둔 사용자가 업데이트 후 바로 다시 로그인하지 않아도 됨).
- `gaitDiagnosticsApi.ts`/`romApi.ts`/`handFootApi.ts`의 모든 함수가 이제 `token`을 첫 인자로 받아 `Authorization: Bearer` 헤더로 보내고, 401 응답은 `AuthError`로 통일해서 던진다.
- `src/shared/components/AppPasswordGate.tsx`(XMSK 전용이던 `XmskPasswordGate.tsx`를 일반화)를 만들고, **토큰 소유권을 `XmskApp.tsx`에서 `App.tsx` 최상위로 옮겼다** — `App.tsx`가 `token` state 하나를 관리하며 `GATED_TABS`(`gait`/`rom`/`handfoot`/`xmsk`) 진입 시 토큰이 없으면 `AppPasswordGate`를 렌더링한다. 홈과 뇌파(EEG)는 이 게이트 밖에 있다 — 홈은 로그인 없이 보여야 하는 랜딩 화면이고, 뇌파는 애초에 서버에 아무것도 저장하지 않아 보호할 개인정보가 없다. 헤더 오른쪽에 "잠그기" 버튼을 추가해 게이트가 걸린 탭에서 로그아웃할 수 있게 했다. 이렇게 최상위로 옮긴 이유: `XmskApp.tsx`가 계속 자기만의 `useState(() => localStorage.getItem(...))`로 토큰을 따로 들고 있었다면, 예를 들어 손발 탭에서 토큰이 만료돼(401) 로그아웃돼도 XMSK 탭은 여전히 로그인된 것처럼 보이는 상태 불일치(state desync) 버그가 생겼을 것이다.
- `XmskApp.tsx`는 이제 `token`/`onLock`을 props로 받는 얇은 컴포넌트가 됐고, 자체 잠금 화면 렌더링은 제거했다. `XmskPasswordGate.tsx`는 더는 어디서도 import하지 않지만(예전 방식 참고용으로) 파일은 지우지 않고 남겨뒀다.
- `HomeScreen.tsx`: 저장된 토큰이 없으면(=로그인 전) ROM 기록 미리보기 fetch 자체를 아예 하지 않도록 고쳤다(`getStoredToken()` 확인 후 없으면 "로그인하면 최근 기록을 볼 수 있어요" 안내만 표시) — 서버가 이제 401로 막아주긴 하지만, 애초에 로그인 안 한 사람에게 그 요청을 보낼 이유가 없다.
- 각 기능의 History 컴포넌트(`GaitDiagnosticsHistory`/`RomSessionHistory`/`HandSessionHistory`/`FootSessionHistory`)와 상위 `*App.tsx`/`*Panel.tsx`가 전부 `token`/`onAuthError` props를 받아 API 401 시 XMSK와 같은 패턴(`onAuthError` → 상위로 전파 → `App.tsx`의 `handleLock`)으로 다시 잠금 화면으로 돌려보낸다.

### Deployment

`Dockerfile` (multi-stage: `npm ci && npm run build` then a slim runtime image) + `docker-compose.yml` for running it. `docker-compose.yml` binds `./data` on the host to `/app/server/data` in the container (where `server/db.ts` puts `rom.db`) so the SQLite file survives container rebuilds/recreation and — since it's just a directory — can be copied wholesale to a different host (Google Cloud, AWS, another PC, …) when migrating; `.env.production` (gitignored, not committed) supplies `XMSK_PASSWORD`/`XMSK_SECRET`. Typical run: `docker compose up -d --build`. The `my-app` service listens on `PORT` (`ENV PORT=8080` in the Dockerfile, matching `EXPOSE 8080`) but is not published to the host directly (`expose`, not `ports`) — a `caddy` service (Caddy 2, config in `Caddyfile`) is the only container publishing ports (80/443) and reverse-proxies to `my-app:8080`, automatically obtaining/renewing a Let's Encrypt cert for whatever domain `Caddyfile` names. `Caddyfile`'s domain is a placeholder (`your-domain.example.com`) — must be edited to the real domain, which must have an A record pointing at the host's (ideally static) IP; the host firewall must allow 80 and 443 in (not 8080, since that's no longer host-exposed).

**`deploy/deploy.sh`의 "zip 최상위 폴더 풀기(unwrap)" 버그(2026-09-14, 실제로 터짐)**: 이 스크립트는 zip을 풀었을 때 최상위 항목이 폴더 하나뿐이면 "감싸는 폴더(예전 myapp.zip류의 `my-app-restructured/`)"로 보고 한 단계 더 풀어서 그 안의 내용물만 VM으로 올리는 로직이 있었다. 문제는, 바뀐 파일이 전부 `src/` 아래에만 있는 zip(예: `src/features/home/HeroSection.tsx`, `.css` 두 개만 담은 zip)도 최상위 항목이 "src" 폴더 하나뿐이라 이 로직에 걸려버린다는 것 — 이럴 땐 "src"가 감싸는 폴더가 아니라 이미 올바른 프로젝트 루트 상대 경로인데도 한 번 더 풀어버려서, 실제로는 `~/my-app/src/features/home/...`가 아니라 엉뚱하게 `~/my-app/features/home/...`로 올라갔다. `docker compose build`는 아무 오류 없이 "성공"하고(원래 `src/`가 안 바뀌었으니 당연히 실패할 이유가 없음) Vite 결과물 해시도 이전 배포와 완전히 동일하게 나와서, 배포 로그만 보면 정상으로 보이지만 실제 사이트엔 변경사항이 전혀 반영되지 않는 조용한 실패였다(히어로 일러스트 재설계 배포에서 발견 — 사용자가 "아직 스틱맨에 변화없음"이라고 보고해서 알아챔). **고침**: 최상위 항목이 하나뿐이어도 그 이름이 `src`/`server`/`package.json`/`CLAUDE.md` 등 이 프로젝트의 알려진 최상위 폴더·파일명과 같으면 "이미 올바른 루트"로 보고 풀지 않도록 `KNOWN_ROOT_ENTRIES` 허용목록을 추가했다. 앞으로 바뀐 파일이 한 최상위 폴더 아래에만 몰려 있는 zip(`src/`만, 또는 `server/`만 바뀐 경우 등)을 만들 때도 안전하다. **교훈**: "배포 로그 성공 = 실제 반영"이 아니다 — 특히 소스가 실제로 바뀌었는데 Vite 산출물 파일명 해시가 이전 배포와 완전히 같다면(둘 다 `dist/assets/index-XXXX.js` 형태로 콘텐츠 해시 기반이라 진짜 내용이 바뀌면 반드시 해시도 바뀜) 그 자체가 "새 소스가 실제로는 빌드에 반영 안 됐다"는 강한 신호이므로, 사용자가 "변화 없다"고 하면 눈으로 보이는 CSS/UI 문제를 먼저 의심하기 전에 **이 배포 경로(스크립트의 파일 복사 단계) 자체를 의심**해야 한다.

### 인프라 개선 — Git 버전 관리·테스트 러너·배포 전 검증 (2026-09)

이 프로젝트는 원래 git 저장소도 아니고 테스트도 전혀 없이, 매번 "바뀐 파일만 zip으로 묶어 scp+ssh"하는 방식으로만 배포돼왔다. GPT-6 아스트라 무료체험 문의를 계기로 "지금 이 프로젝트에 실제로 도움이 되는 게 뭔지" 논의하다가, 병목은 AI 모델 성능이 아니라 이 인프라 공백(버전 이력 없음, 회귀를 자동으로 잡아주는 게 없음)이라는 결론이 나와 사용자가 명시적으로 개선을 요청했다. 세 가지를 확인 질문으로 미리 정했다: ① git 저장소 위치는 VM에만(GitHub 등 새 외부 계정 연결 없이), ② 테스트는 순수 계산 함수만(컴포넌트/캔버스/블루투스 같은 브라우저 API 의존 코드는 범위 밖), ③ 배포 자동화는 지금의 "Cloud Shell에 붙여넣기" 흐름을 그대로 두고 그 안에 배포 전 검증만 추가(완전 자동 CI/CD처럼 GCP 인증정보를 공유해야 하는 방식은 채택하지 않음).

- **Git — 왜 GitHub가 아니라 VM 로컬 저장소인가**: 이 세션(Claude)이 실행되는 컨테이너는 세션이 끝나면 사라지는 임시 환경이라, 여기에 `git init`만 해두면 그 히스토리도 같이 사라진다 — 버전 관리가 실제로 의미가 있으려면 항상 살아있는 곳에 있어야 한다. GitHub 원격 저장소를 새로 연결하는 방법도 있지만 계정 연결·인증 토큰 설정이 필요해 지금의 "복사-붙여넣기만으로 배포" 흐름에 마찰을 더한다. 그래서 실제로 코드가 계속 살아있는 곳 — 배포 대상 VM의 `~/my-app` 자체 — 를 git 저장소로 만들었다. 새 계정이나 인증정보가 전혀 필요 없고, 지금 워크플로우(Cloud Shell에 `bash deploy.sh` 붙여넣기)에 아무 추가 단계도 필요 없다. (나중에 GitHub 백업도 원하면 VM에서 `git remote add origin ...`/`git push`만 추가하면 되므로, 이 선택이 나중의 확장을 막지 않는다.)
- **동작 방식**: `deploy/deploy.sh`(v5)가 scp로 바뀐 파일을 VM에 올린 뒤, `docker compose build`를 돌리기 전에 SSH로 VM에 들어가 `~/my-app`에서 (최초 1회만) `git init`하고, 이번에 바뀐 파일들을 `git add -A && git commit -m "배포: <zip 파일명> (<시각>)"`로 커밋한다. 커밋할 변경사항이 없으면(zip 재실행 등) 조용히 건너뛴다. 이제부터 모든 배포가 자동으로 이력에 남으므로, 문제가 생기면 `ssh`로 VM에 들어가 `git log --oneline`으로 배포 이력을 보고 `git checkout <커밋> -- . && docker compose build && docker compose up -d`로 특정 시점으로 되돌릴 수 있다 — 예전에는 "며칠 전 버전으로 되돌리기" 자체가 불가능했다.
- **테스트 러너 — vitest, 왜 순수 계산 함수부터인가**: 이 앱의 핵심 로직 상당수(ROM/보행/손발 관절 각도, RMSSD 등)는 DOM이나 카메라 없이 입력→출력만으로 검증 가능한 순수 함수라, 여기부터 테스트를 붙이는 게 비용 대비 가장 효과적이라고 판단했다(캔버스/비디오/Web Bluetooth 흉내는 jsdom으로도 불완전하고 유지보수 비용이 큼). `vitest.config.ts`(프로젝트 루트, `vite.config.ts`와 완전히 분리된 별도 설정 — 테스트 대상 함수들은 React나 `@mediapipe/*` 셔밈을 전혀 안 쓰므로 그 설정이 필요 없음)가 `src/**/*.test.ts`만 수집한다. 처음 도입한 테스트: `shared/lib/mathUtils.test.ts`(mean/stddev/movingAverage — ROM 스무딩 전제 자체도 검증), `shared/lib/heartRate/heartRateInterpretation.test.ts`(RMSSD 계산, XCTS의 심박/HRV 방향 분류), `features/rom/lib/jointAngles.test.ts`(관절 각도 기하 공식), `features/rom/lib/romAnalysis.test.ts`(스무딩+min/max+delta 전체 파이프라인을 합성 각도 시퀀스로 검증). 다른 기능(보행·손발·XCTS의 나머지 계산)에도 같은 패턴을 그대로 확장할 수 있다.
  - **⚠️ vitest 자체는 이 개발 샌드박스에서 실제 설치·버전 검증을 못 했다** — `package.json`의 `"vitest": "^3.2.0"`은 muse-js/`@tensorflow-models/hand-pose-detection`을 추가할 때와 같은 사정(이 샌드박스는 npm 레지스트리·unpkg 접근이 막혀 있음, 위 손·발 분석 섹션 참고)으로 registry 메타데이터를 직접 확인하지 못하고 넣은 값이다. VM 빌드(`npm install`, 실제 인터넷 접근 가능)에서 버전 충돌이나 peer dependency 경고가 뜨면 그 로그를 보고 버전을 조정하면 된다 — 다만 npm 7+ 는 peer dependency 불일치를 기본적으로 경고만 하고 설치를 막지는 않으므로(strict 모드가 아닌 한) 실패할 가능성은 낮다고 본다.
- **배포 파이프라인에 테스트 연결 — `Dockerfile`**: 완전 자동 CI/CD(GitHub Actions 등)는 채택하지 않기로 했으므로(GCP 인증정보를 공유해야 하는 리스크), 대신 이미 있는 빌드 단계에 검증을 끼워 넣었다 — `Dockerfile`의 build 스테이지에서 `COPY . .` 직후, `npm run build`보다 먼저 `RUN npm test`를 추가했다. 테스트가 하나라도 실패하면 `docker build`가 그 자리에서 실패하고, `deploy.sh`의 `docker compose build && docker compose up -d`는 `&&`로 묶여 있으므로 빌드가 실패하면 `docker compose up -d`(실제 서비스 재기동)가 아예 실행되지 않는다 — 즉 테스트 실패가 곧 배포 중단이다. 새 GCP 서비스 계정, SSH 키 공유, GitHub Actions 설정 같은 것 없이, 지금 워크플로우 그대로("bash deploy.sh 붙여넣기") 안전장치만 하나 늘어난 셈이다.
